# 更新履歴

たすきば Knowledge Relay の変更内容を記録します。形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、バージョン採番は [Semantic Versioning](https://semver.org/lang/ja/) に従います。

カテゴリ:

- **追加** (Added) — 新機能
- **変更** (Changed) — 既存機能の改善
- **修正** (Fixed) — 不具合修正
- **削除** (Removed) — 削除された機能

---

## [1.2.0] — 2026-06-12

WBS（タスク）の **一括削除を大幅に高速化** し、あわせて **上書きインポートを安定化**（追加・更新の多い大きな WBS でも途中で時間切れにならず取り込めるよう修正）しました。タスクが多いプロジェクトでも、まとめて削除・取り込みする操作が短時間で完了します。あわせて、**新しい組織を開設するときのサインアップ画面を、はじめての方でも迷わず入力できるよう改善** しました。さらに、**他サービスから API でつないでデータを移行する機能をベータ提供** しました。加えて、各資産の本文に書いた **URL（http / https）を表示時に自動でクリックできるリンク** にする機能を追加し、**タスクに「備考」欄を新設** しました。さらに、プロジェクト詳細に **「分析」タブを新設**し、**予定と実績の進捗カーブ・担当者別の消化工数/作業負担/日次工数など 5 種類のグラフ** を PM/PL・テナント管理者向けに提供します。また、**見積もりに「手動登録」と「ツールで見積もる」の 2 モードを追加**し、WinActor・kintone・OutSystems など RPA/ローコードツール向けの係数計算や Excel/Word エクスポートを提供します。

### 追加

- **プロジェクトに「分析」タブを新設（予定 vs 実績の進捗グラフ）**: プロジェクト詳細画面に **分析** タブを追加し、**5 種類のグラフ**を表示できるようにしました。プロジェクトマネージャ／リーダーが「完了に向けて今どこまで進んでいるか（現在地）」と「どのくらいのペースで消化できているか（生産性）」、「誰にどれだけ作業が残り、いつ無理が出そうか（負荷）」をひと目で把握するための機能です。
  - **① 進捗の遅れ・先行（予実カーブ・折れ線）**: 横軸は日付、縦軸は WBS のアクティビティ（実作業）の完了割合（0〜100%）。**実線が予定**（予定完了日に到達したアクティビティの累積割合）、**点線が実績**（実際に完了したアクティビティを実績完了日で累積した割合）です。2 本の線の開きがスケジュールの遅れ／先行を表します。本日位置の縦線と「本日時点 予定◯% / 実績◯%（遅れ◯%）」の要約も表示します。
  - **② 消化ペースと効率（担当者別 週次消化工数・積み上げ棒）**: 横軸は週、縦軸は**実績工数（人時）**で、1 本の棒を担当者ごとに色分けして積み上げます。件数ではなく工数で測るため、**タスクの大小を吸収**して「誰がいつどれだけの作業量をこなしたか」が分かります。グラフ上部には**工数効率（予定工数 ÷ 実績工数。1 超で効率的）**も表示します。担当者が多い場合は上位 8 人を個別色、残りは「その他」、担当者なしは「未割当」とします。なお工数効率は「見積に対する効率」の目安であり、個人の評価・比較には使わないことを推奨します。
  - **③ 見積の精度（予実差）（担当者別 予定 vs 実績 工数・グループ棒）**: 担当者ごとに、**PM/PL が記載した予定工数**と**担当者が記載した実績工数**を横並びで比較します。「誰が見積より工数が掛かる／掛からない傾向か」が分かり、次回の見積もりやタスクの割り振りの参考になります。棒にカーソルを合わせると差（実績 − 予定）も表示されます。
  - **④ 作業量の偏り（担当者別 作業負担・状態別 積み上げ棒）**: **今、誰にどれだけ作業が残っているか（総量）**を担当者ごとに比べます。未完了タスク（未着手・進行中。保留中は除く）の予定工数を状態別に積み上げ、できる人への偏りを防いだり、負荷分散・割り振りを検討したりする材料になります。右上の切替で「割り振り量」（予定工数そのもの）と「個人ペース補正」（各担当者の実績÷予定比を掛けた予想残工数。得意・不得意やスピード差を織り込む）を切り替えられます。平均・最も重い人・最も軽い人も表示します。なお予定工数は見積もりのため、正確な負担量や個人の評価ではなく割り振り検討の材料としてご利用ください。
  - **⑤ 日別の負荷（8h超）（担当者別 日次工数・ヒートマップ）**: 未完了タスクの予定工数を **1 日ずつに均等に割り振り**、各担当者（行）の **1 日あたりの工数** が日付（列・本日以降）ごとに上限 8 時間に達していないかを **表（ヒートマップ）** で一覧します。各マスはその日の予定工数（時間）で、**8 時間以内は無色・7 時間超は黄色（警告）・8 時間超は赤色（超過）** に色分けされます。④（残りの総量）に対し、⑤は **「いつ 1 日のピークが来るか（山積み）」** を見るためのもので、総量が同じでも短い期間に詰め込まれていれば赤くなります。予定の前倒し／後ろ倒しや担当の振り直しなど、負荷の平準化を検討する材料になります。割り振りは予定期間で均等に割った概算で、土日祝も 1 日として割り振ります（完了・保留中のタスクは含みません）。同じ作業でも人によりかかる時間は違うため、負荷の偏りに気づくための目安であり、個人の評価・比較には使わないでください。
  - **表示するグラフの選択と対象期間の絞り込み**: 分析タブ上部にツールバーを追加しました。**表示するグラフをボタン（チップ）で選べ**（複数選択。**初期は「WBS 予実カーブ」1 枚だけ**表示するので、見たいグラフを足していきます）、**対象期間**（全期間／直近1・3・6ヶ月／カスタムの開始日〜終了日）で絞れます。期間を選ぶと表示範囲だけでなく **効率・合計・順位などの数値もその期間で計算し直します**。期間はグラフの性質に合わせて適用され、振り返り系（予実カーブ・週次消化工数・予実差）は過去側、日次工数は今後側に効き、作業負担は「今この瞬間」のため期間の影響を受けません（注記表示）。選んだ表示グラフ・期間は同じブラウザで開いている間は記憶されます（別ユーザーには引き継がれません）。
  - **あわせて WBS（タスク）に「実績工数」欄を新設**: アクティビティ（ACT）の実績入力に **実績工数（人時）** を追加しました。担当者が実績を入力する際にあわせて入力でき、上記②③のグラフ・工数効率に反映されます。**一括実績更新（複数タスクをまとめて更新）でも実績工数を対象に含められます**。
  - **「完了」にするタスクは実績工数の入力を必須化**: ステータスを「完了」にする（または進捗率を 100% にする）タスクは、**実績工数（人時）を 0 より大きい値で入力することが必須**になりました（単体・一括の両方）。これにより、消化工数・工数効率の集計が欠けなく行えます。一括で完了にする場合は、実績工数を一括指定するか、対象タスクに先に実績工数を入力してください。
  - **表示できる人**: **プロジェクトの PM/PL、またはテナント管理者のみ**（一般メンバーには表示されません）。完了済みプロジェクトでも、振り返りのために閲覧できます。
  - **データ源**: WBS に入力済みの「予定完了日」「実績完了日」「完了ステータス」「担当者」「実績工数」から自動で集計します（実績が未入力のうちはグラフが伸びません）。
- **外部データ API 連携インポート（ベータ）**: CSV を用意しなくても、**Notion / Backlog / kintone / Pleasanter / Google スプレッドシート** に直接つないで、顧客・プロジェクト・WBS・リスク・課題・ナレッジ・振り返りを取り込めるようにしました（テナント管理者向け。設定画面 →「データインポート」→「② 外部データを直接インポートしたい方へ（ベータ）」）。
  - **使い方**: サービスを選んで接続 → 取得元（データベース・アプリ・サイト・シート）と項目を読み込み → たすきばの項目に割り当て → プレビューで件数・エラーを確認 → 取り込み。取り込みの仕組み（選択肢・日付の書き方、1 回 5,000 件まで、下書きは追加料金なし、新規作成のみ）は CSV の「外部データ移行」と共通です。
  - **安全面**: 接続に使うトークン／API キーはサーバに保存しません（接続時とプレビュー時に入力し、送信直後に破棄）。閲覧（読み取り）権限のトークンで利用できます。
  - **取り込み前に長すぎる項目を検知**: 名称など文字数に上限がある項目は、超過していればプレビューでお知らせします（黙って切り詰めません）。
  - ベータ機能のため、まずは少量で試してから本番データを取り込むことをおすすめします。
- **画面上部のお知らせ帯（周知バナー）**: メンテナンスや障害など、全利用者にすぐ気づいてほしい連絡を、画面の一番上に **一定期間だけ** 帯で表示できるようになりました。
  - **重要度で色分け**: 高＝赤 / 中＝黄 / 低＝青 で塗り分け、ひと目で重要度が伝わります
  - **表示期間を指定**: 「いつからいつまで」を決めて表示し、**期間が終わると自動で消えます**
  - **「✕」で閉じられます**: 閉じた状態はログイン中のあいだ保持され、いったんログアウトして再ログインすると（表示期間内なら）もう一度表示されます（大切な連絡の見落とし防止）
  - 帯の作成・配信ができるのは **たすきば運営（システム管理者）のみ** です。同時に表示される帯は 1 本までで、過去に出した帯は履歴として残り、複製して再利用できます
- **本文中の URL を自動でリンク化**: ナレッジ・リスク・課題・振り返り・プロジェクト概要・メモ・タスク（説明／備考）・顧客の備考・コメント・画面上部のお知らせ帯など、各資産のテキスト欄に `https://…` や `http://…` で始まる URL を書くと、**表示するときに自動でクリックできるリンク**（新しいタブで開く）になります。
  - **日本語の直前・直後でも URL 部分だけを正しくリンク化** します。たとえば「`https://example.com/login`にアクセスしてください。」と続けて書いても、`https://example.com/login` だけがリンクになり、後ろの日本語や句点（。）はリンクに含まれません
  - 文末のピリオド・読点や、文章側の括弧（例: 「（詳しくは https://example.com ）」）も URL に巻き込まずに正しく切り分けます
  - 特別な書き方は不要です。これまでどおり URL を貼り付けて保存するだけで、表示が自動でリンクになります
- **タスクに「備考」欄を新設**: アクティビティ（ACT）に、作業内容（説明）とは別に補足メモを残せる **備考** 欄を追加しました。関連資料の URL などを書いておくと、上記のとおり自動でリンク化されます（任意・最大 1,000 文字）。作成・編集ダイアログから入力でき、プレビューで表示イメージを確認できます。
- **見積もりに「手動登録」と「ツールで見積もる」の 2 モードを追加（PM/TL 向け）**: 見積もり画面に 2 つの作成パターンを追加しました。
  - **手動で登録**: 確定済みの工数を直接入力する従来相当のパターン。「手動」バッジで一覧に表示されます。
  - **ツールで見積もる（係数計算）**: 開発ツール・規模・難易度を選ぶと工数をリアルタイムで自動計算する新パターン。計算式は `基準時間 × 規模係数 × 難易度係数 × 手法係数`。「係数」バッジ + 使用ツール名が一覧に表示されます。
  - **対応ツール（全 16 種）**: スクラッチ開発 / パッケージ / WinActor / UiPath / Power Automate Desktop / Power Apps / kintone / Pleasanter / OutSystems / Salesforce Lightning / ServiceNow / Mendix / AppSheet / Zoho Creator / その他。
  - **区分の追加**: 工程区分に「レビュー」「管理」を追加し 7 種類になりました（要件定義 / 設計 / 開発 / 試験 / レビュー / 管理 / その他）。
  - **工数サマリパネル**: 画面上部にカテゴリ別小計・バッファ率設定（0〜50%、デフォルト 20%）・バッファ込み合計を表示するサマリパネルを追加しました。
  - **Excel / Word エクスポート**: サマリパネルから工数見積もりを Excel（サマリ + 明細の 2 シート）または Word（表紙 + 工数サマリ + 見積明細 + 備考一覧）でダウンロードできます。
  - 将来機能（案Y）: テナントレベルのカスタムプリセット値設定は今後の拡張として計画中です。

### 変更

- **多言語対応の基盤整備（Phase 1）**: UI 文言・エラー文言を `src/i18n/messages/` のメッセージカタログに一元管理する基盤を整えました。今回のリリースで、主要な共通コンポーネント（編集ダイアログ・添付・通知トースト等）とプロジェクト系画面（プロジェクト一覧・詳細・メンバー・見積もり・リスク／課題・振り返り・ナレッジ・ステークホルダー・参考提案）のメッセージを日本語／英語のカタログから読み込むようにしました。ご利用中は従来どおり日本語で表示されますが、お客様の言語設定（テナント設定 → 言語）を英語にすると、対応済みの画面・操作で英語表示に切り替わります。今後のリリースで、テナント管理・設定画面・WBS（タスク）画面・各種メール・ヘルプ／使い方ガイドなど残りの画面と機能を順次対応していきます。
  - **退行防止の自動化**: 新たに混入したハードコードの日本語文字列を CI で機械的に検出するチェックを追加しました（`scripts/check-no-hardcoded-jp.ts`）。今後の機能追加でも翻訳カタログ経由の実装が徹底されます。
  - **エラーコード体系**: 内部のエラーを安定識別子（`TENANT_NOT_FOUND` 等）で扱う `AppError` 基盤を導入し、API レスポンスのエラーが言語設定に追従して表示されるようになりました（対応済 API のみ。残りは順次移行）。
  - **影響範囲**: 既存の日本語ユーザの表示は変わりません。データベース・課金・認証などコア機能の動作も変更ありません。
- **テナント開設（サインアップ）をより始めやすく改善**: 新しい組織を開設するときの入力負担を減らしました。
  - **組織 ID の入力を不要に（自動採番）**: これまで自分で考えて入力する必要があった「組織 ID（ログイン時に使う識別子）」を、**システムが自動で割り当てる**ようにしました（数字の ID。例: 100001）。入力欄そのものを無くし、考える手間をゼロにしました。割り当てられた組織 ID は、サインアップ完了画面と招待メールでお知らせします（組織 ID は作成後は変更できません）。
  - **入力の並び順を最適化**: 「組織情報 → お名前・メール → プラン選択 →（有料プランのときだけ）請求先情報」の順に並べ替えました。無料の Beginner プランでは請求先情報の入力欄は出ないため、より少ない入力で開設できます。
  - **送信後の案内を充実**: 「招待メールを送信しました」の画面に、届くメールの差出人（noreply@tasukiba.com）・件名（「たすきば - アカウントの設定」）・到着の目安（通常 1 分以内）・このあとの手順を **届く前に** 表示し、メール待ちでの不安や迷子を減らしました。
- **WBS 一括編集・一括実績更新の件数上限を撤廃**: 複数タスクをまとめて更新する「一括編集（担当者・優先度・予定日程・予定工数）」と「一括実績更新（ステータス・進捗率・実績日程）」の件数制限を廃止しました。従来は 100 件を超えると「一括更新は100件までです」エラーになっていましたが、大規模プロジェクト（600 件以上）でも問題なく使えるようになりました。内部では 100 件ずつのチャンクに自動分割して処理するため、タイムアウトの心配はありません。
- **WBS（タスク）の一括削除を高速化**: 複数のタスクを選んで一度に削除する操作を見直し、まとめて処理するようにしました。これまでは選んだ件数が増えるほど時間がかかり、1 件あたり数秒かかることもありましたが、件数が多くても短時間で完了します。
  - 件数が非常に多い場合は **自動的に分割して処理** するため、途中で時間切れ（タイムアウト）になりにくくなっています
  - 削除の一部が失敗した場合は、**失敗した分だけを安全にやり直せます**
  - 削除後の作業グループ（WP）の集計（工数・進捗など）の再計算は、一括削除の最後にまとめて 1 回行うよう最適化しました
- **タスクの一括更新も高速化**: 複数のタスクをまとめて更新（担当者・期限・状況・実績などの一括変更）する際の集計の再計算を見直しました。対象のタスクが**多くの作業グループ（WP）にまたがる**場合に処理が重くなっていたのを改善し、結果（集計値）はこれまでと変わらず短時間で完了します。
- **タスクを 1 件ずつ操作するときの内部処理も最適化**: タスクの追加・編集・削除を 1 件ずつ行う際にも、内部で同じ情報を二重に読み取っていた無駄や、不要な集計の書き込みを削減しました。表示や記録・集計の内容はこれまでと変わらず、操作の応答が少し軽くなります。
- **タスクの「作業内容（説明）」をプレビュー対応に**: 説明欄に Markdown プレビューと差分表示を追加し、入力した URL がリンクとして表示されることをその場で確認できるようにしました（他の資産の本文欄と操作感をそろえています）。
- **クローズしたプロジェクトを完全な「読み取り専用」に統一**: クローズ済みのプロジェクトでは、リスク・課題・ナレッジ・振り返り・WBS（タスク）・ステークホルダーの **追加・編集・削除・コメント投稿ができないように統一** しました。これまで一部の画面で編集ダイアログや削除ボタン、コメント投稿欄が表示されて操作できてしまう場合があったのを、画面から隠すとともに、サーバ側でも受け付けないよう二重で防止しています（プロジェクト自体の削除のみ、従来どおり可能です）。
- **振り返り一覧から「ステータス」列を削除**: 振り返り一覧（プロジェクト内・全振り返りの両画面）で、意味が薄くなっていた「下書き／確定」のステータス列を非表示にし、画面を見やすくしました（確定の操作自体は、各行の操作から引き続き行えます）。

### 削除

- **旧「外部データ移行ウィザード」の廃止**: ナレッジ・課題のみを取り込む旧ウィザードを廃止し、v1.1.0 で追加した **7 種類対応の「① CSVファイルをインポート」に一本化** しました。CSV からの一括取込はすべて新しい画面（設定 →「データインポート」→「① CSVファイルをインポートしたい方へ」）からご利用ください（旧ウィザードは画面の案内から外れており、機能は新画面が引き継いでいます）。
- **WBS 管理画面の「IDを表示」「工数集計」ボタンを削除**: 利用頻度が低く画面のスペースを占有していた 2 つのボタンを削除しました。「IDを表示」はデバッグ・確認用として設けていたもので、CSV インポート時の ID 照合は CSV エクスポートで直接確認できます。担当者ごとの日次工数の確認は ACT 編集ダイアログ内の工数プレビュー（作成・編集時に自動で表示）をご利用ください。

### 修正

- **WBS（タスク）の上書きインポートが、追加・更新の多い大きな WBS で途中失敗する不具合を修正**: 確定実行が時間切れ（タイムアウト）になり「インポートに失敗しました」と表示される場合があったのを解消しました。内部処理（既存タスクの更新と、作業グループ（WP）の集計再計算）をまとめて一括で行うよう見直し、追加・更新が多い WBS でも短時間で完了します。
  - 1 回で取り込める上限は **2,000 行** です（これを超える場合は CSV を分割してください）。
  - 万一エラーで取り込みきれなかった場合でも、**同じ CSV をもう一度取り込めば安全に最後まで反映** されます（各行を ID で突き合わせるため、反映済みの行は二重に作られません。ID が空の新規行のみ再取込のたびに作成されます）。
  - 取り込み結果（タスクの値・WP の集計）はこれまでと変わりません。
- **たすきフクロウ（AI ヘルプ）の回答内容を実装に合わせて正確化**: 公開範囲（下書き／公開の 2 種類）・提案エンジンの対象条件（リスク・課題は「公開」かつ状態が「解消」）・データ容量超過時の挙動（Expert / Pro は止まらない）・お支払い期限・二段階認証の復旧手順（リカバリーコード再発行）・見積もりの入力項目・一般メンバーの権限などの説明を、最新の仕様に合わせて修正しました。
- **たすきフクロウの知識をさらに拡充**: 意味検索・AI 検索用データの料金やヘルプチャットの無料枠（全プラン無料・月 100 回）、各画面の入力項目（振り返りの計画総括など）、リスク・課題のステータス（未対応／対応中／監視中／解消）の意味、ログインロックの解除方法などの FAQ・使い方ガイドを追加しました。
- **プロジェクト別ナレッジ一覧の「更新者」が空欄になる不具合を修正**: プロジェクトの「ナレッジ一覧」タブで、ナレッジを更新しても更新者の名前が表示されず「—」のままになる場合があったのを修正しました（更新者のデータ自体は正しく記録されており、一覧の表示だけの問題でした。全ナレッジ画面では正しく表示されていました）。
- **使い方ガイド・設計ドキュメントの最新化**: 上記の機能統合・仕様に合わせて、外部データ取込ガイドや見積もりガイド、関連ドキュメントを更新しました。

---

## [1.1.0] — 2026-06-05

一覧画面の **UI を大幅刷新** し、操作性と画面間の一貫性を高めました。あわせて **FAQ・使い方ガイドを大幅拡充** し、AI ヘルプ「たすきフクロウ」がこれまで答えられなかった領域までカバーできるよう賢くなりました。セキュリティ面では「他の端末からログアウト」を新設しています。さらに **WBS（タスク）の上書きインポートを改善** し、同じ名前のタスクや件数の多い WBS でも安定して取り込めるようにしました。

### 追加

- **他の端末からログアウト** (設定画面): 現在使用中の端末はログインしたまま、他のすべての端末・ブラウザを一括ログアウトできます。端末の紛失・盗難、共用 PC への置き忘れ、不正アクセスが心配なときの自己防衛手段です
- **メモへのファイル添付**: メモでも他の資産 (プロジェクト・タスク・リスク・課題・振り返り・ナレッジ) と同様に、外部リンク (URL) に加えてファイル本体を添付できるようになりました (1 ファイル 50MB)
- **たすきフクロウ (AI ヘルプ) の知識を大幅拡充**: マイタスク・メモ・個人設定・セキュリティ (二段階認証・パスワード・他端末ログアウト) など、これまで回答できなかった領域の FAQ・使い方ガイドを多数追加し、質問への回答精度が向上しました
- **スターターデータの取り込み・削除** (設定画面): データが何も無い状態でも、提案機能や AI チャット検索の便利さをすぐに体験できるよう、見本の顧客・プロジェクト・課題・リスク・ナレッジ・振り返りを **1 クリックで取り込める** ようになりました。お試し後は **一括削除** でき、手動で追加・編集した通常のデータは残ります。プロジェクト一覧が空のときにも案内リンクを表示します
- **外部データ移行 — 手動 CSV 一括取込** (設定画面 →「データインポート」): 他ツールや Excel に蓄積した **顧客・プロジェクト・WBS（タスク）・リスク・課題・ナレッジ・振り返り** を、種類ごとの CSV から **一括で新規取り込み** できるようになりました。初回移行用の機能で、すべて新規作成されます（既存データの一括更新は各「○○一覧」から）。
  - **テンプレ CSV ダウンロード → マッピング → プレビュー → 取込** の 4 ステップ。種類ごとの日本語ヘッダ付き空 CSV を配布。列を画面項目に割り当て、顧客・プロジェクトは「名前」で親子を紐づけます
  - **入力チェックを充実**: プルダウン項目は選択肢の値のみ受け付け（選択肢外はプレビューでエラー）、日付は **YYYY-MM-DD と YYYY/MM/DD のどちらも可**（Excel 既定のスラッシュ区切りでも設定変更なしで取り込み可）、WBS の予定工数は小数第一位まで、などをプレビューで分かりやすく案内します
  - リスク・課題は 1 つの CSV にまとめ、種別（リスク / 課題）に応じて **影響度／重要度・発生可能性／緊急度** の列を使い分けます
  - **「取り込むと参照できなくなるデータ」を未然に防止**: どのプロジェクトにも紐づかない下書きの資産は一覧に出ないため、プレビューでエラーにして「プロジェクトを指定するか公開にする」よう案内します
  - データ行が無い・列割当が未設定などの場合も、プレビュー画面まで進んで **理由を表示**（分かりにくいエラーで止まりません）
  - AI 検索用の embedding は **1 回の取り込み＝まとめて 1 回分** で一括生成するためコスト効率が高く、既定の「下書き」で取り込んだ分は生成・課金されません

### 変更

- **テナント開設フォームの簡素化**: 無料の Beginner プランでお試し開設するとき、住所などの請求先情報の入力が **不要** になりました。気軽にテナントを作って試せます (有料の Expert / Pro プランでの開設、または後から有料プランへ変更するときは、従来どおり請求先情報が必要です)
- **提案・AI チャットの参照範囲をより安全に**: 提案機能と AI チャット検索が、運営の見本データを横断的に参照する仕組みをやめ、**各テナント自身のデータのみ** を参照するようになりました。テナント間のデータ分離がさらに強化されています (見本データはスターターデータの取り込みでご利用ください)

- **一覧画面の UI 刷新・統一**:
  - **メモ一覧 / 全メモ**: 列構成を見直し、外部リンクとファイル添付をそれぞれ別の列に分離。作成日時の列を追加し、他の一覧画面と並びをそろえました
  - **メモ一覧**: 作成ボタン・絞り込み・一括編集・列幅リセットの配置を、他の「○○一覧」画面と統一しました
  - **マイタスク**: 列構成を WBS / タスク画面とそろえ、見やすく整理しました
- **WBS の上書きインポートの改善**:
  - 同じ作業グループ（WP）の下に **同じ名前のタスクを複数取り込めるように** しました（例: 毎週繰り返す学習タスク）。プレビューには同名がある旨のお知らせが出ますが、そのまま取り込め、それぞれ別タスクとして作成されます
  - **取り込み件数のハード上限を撤廃** しました。件数が多い場合は「処理に時間がかかる場合がある」旨をプレビューでお知らせします（取り込みはそのまま実行できます）
  - 大きな WBS でも安定して取り込めるよう、取り込み処理を高速化しました

### 修正

- **WBS の上書きインポートで、警告のみ（エラーなし）なのに取り込みが失敗していた不具合** を修正しました。具体的には次の 2 つの原因を解消しています:
  - 同じ作業グループ（WP）の下に同じ名前のタスクがあると、確定実行時に失敗していた
  - タスク件数が多い WBS で、確定実行が時間切れになり失敗していた

### 削除

- **メモ一覧 / 全メモの「公開範囲」列**: 公開範囲は編集画面と一括変更で操作する運用に統一し、一覧の列からは撤去しました
- **マイタスクの「優先度」列**: WBS / タスク画面と表示をそろえるため撤去しました
- **メモ一覧の画面見出し**: 他の一覧画面と体裁を統一するため撤去しました
- **設定画面の「提案エンジン: シードデータ参照」トグル**: 提案・AI チャットが各テナント自身のデータのみを参照する方式に変わったため撤去しました。見本データを使いたい場合は新しい「スターターデータの取り込み」をご利用ください

---

## [1.0.0] — 2026-06-01

一般提供開始 (GA) リリース。プロジェクトの企画 → 完了 → 振り返りまでを一気通貫で支援する基盤と、過去資産を意味検索で再利用する **提案エンジン** を核心機能として提供します。

### 追加

- **プロジェクト運営**: プロジェクト管理 (CRUD + 7 状態の遷移マシン)、見積もり管理、WBS / タスク管理 (階層構造)、ガントチャート、進捗・実績更新、リスク・課題管理
- **提案エンジン (意味検索)**: 過去のナレッジ・リスク・課題・振り返り・メモを 5 資産横断で意味検索し、スコア順に段階表示。Voyage AI embedding + pgvector で「キーワードが違っても意味が近いもの」を取りこぼさず提示
- **チャットボット意味検索**: 画面右下の FAB から自然文で質問し、5 資産横断で関連情報を取得
- **ナレッジ管理**: 3 段階公開範囲 (自分のみ / 全メンバー)。LLM 自動タグ抽出 (Anthropic Claude) で投稿コストを最小化
- **振り返り**: プロジェクト終了時の Keep / Problem / Try 記録、次案件での再利用
- **マイタスク**: プロジェクト横断のタスク一覧、優先度・期日順表示
- **メモ**: 個人ノート。公開化することで提案エンジンの候補に昇格
- **テナント管理**: 設定 3 タブ (概要 / 使用量 / 請求)、利用量リアルタイム表示、月次予算上限設定
- **課金**: per-call 課金 (Beginner プランはプロジェクト作成・更新が月 50 回まで無料 + 縮退モード / Expert ¥10/call / Pro ¥15/call、ADR-0019 改定後)、DB 容量超過 ¥50/GB tier・ファイル容量超過 ¥10/GB tier (ADR-0020 / 0021)、銀行振込・請求書支払い対応
- **セキュリティ**: メール + パスワード認証、TOTP MFA (admin 必須・一般任意)、アカウントロック、監査ログ、認証イベントログ、テナント越境防止
- **運用ツール**: super_admin ダッシュボード、テナント管理、利用量モニタ、DB 容量モニタ、データエクスポート
- **公開ページ**: `/changelog` (本ページ)、`/announcements` (お知らせ)、`/settings/about` (サービス情報) を追加。全画面共通フッタでバージョン・運営者・更新履歴への導線を常時表示

### 既知の制約

- 適格請求書発行事業者として登録していません (経過措置をご活用ください)
- 開発者 1 名 + Claude Code 中心の少人数体制のため、明示的な SLA は提示していません
- 上位プランから Beginner プランへのダウングレードは不可 (個別対応はお問い合わせください)
- 多言語対応 (en-US) は足場のみ。本格対応は v1.x 以降

詳細: [docs/archive/2026-06-01-pre-ops-reorg/RELEASE_NOTES_v1.md](docs/archive/2026-06-01-pre-ops-reorg/RELEASE_NOTES_v1.md)

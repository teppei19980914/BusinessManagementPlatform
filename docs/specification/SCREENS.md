# 画面仕様 (Specification) — 全画面網羅 (実装ミラー)

本ドキュメントは、本サービスの **全画面** (dashboard 40 + auth 5) の機能仕様を集約した **実装ミラー** である。開発者がこの 1 ファイルで全画面の目的・操作・UI 要素・権限・関連 route/service を把握できることをゴールとする。画面別権限マトリクスは [PERMISSION_MATRIX.md](./PERMISSION_MATRIX.md)、共通 UI 制御ルールは [UI_RULES.md](./UI_RULES.md) を参照。

> **スコープ (2026-05-31 拡張)**: 従来は「主要 12 画面」(§11.1〜§11.12) のみを記載していたが、本改訂で `src/app/(dashboard)/**/page.tsx` と `src/app/(auth)/**/page.tsx` の全実装を inverse coverage で網羅した。詳細な入力項目・状態×ロールのボタン制御マトリクスは主要 12 画面 (§11) を、それ以外の画面 (super_admin / admin / 設定 / ヘルプ等) は **§0 全画面インベントリ** を参照すること。

> 🆕 **ADR-0019 (2026-05-24) 価格改定**: 課金対象は `BILLABLE_FEATURE_UNITS` (project-upsert / suggestion-explanation / auto-tag-extract) のみ。資産入力 (Knowledge / RiskIssue / Retrospective / Memo) / チャット検索 / CSV インポートは全プラン無料化。Expert ¥10/call、Pro ¥15/call、Beginner プロジェクト作成/更新 月 50 回まで無料。本仕様内の単価・上限の最新値は [ADR-0019](../adr/0019-billable-feature-units-and-free-tier-expansion.md) を優先参照。

---

## §0. 全画面インベントリ (実装ミラー)

`src/app` のルート構成を真値とした全画面一覧。**カテゴリ別件数**:

| カテゴリ | 画面数 | 代表ルート |
|---|---:|---|
| 認証 (auth, 未ログイン) | 5 | `/login` `/login/mfa` `/signup` `/reset-password` `/setup-password` |
| ダッシュボード入口 | 1 | `/` (→ `/projects` へ redirect) |
| プロジェクト系 | 9 | `/projects` ほか detail 配下タブ |
| プロジェクト横断 (資産) | 6 | `/risks` `/issues` `/knowledge` `/retrospectives` `/memos` `/all-memos` |
| 個人 | 2 | `/my-tasks` `/memos` |
| 顧客管理 (admin) | 2 | `/customers` `/customers/[id]` |
| 運用管理 (admin) | 3 | `/admin/users` `/admin/audit-logs` `/admin/role-changes` |
| 設定 | 3 | `/settings` `/settings/tenant` `/settings/tenant/billing` |
| データ移行 (admin) | 2 | `/settings/tenant/migration-import` `/settings/tenant/api-import` |
| ヘルプ・ガイド | 2 | `/help` `/guide` |
| 運営者専用 (super_admin) | 15 | `/admin/super` 配下 |
| **合計 (dashboard 43 + auth 5)** | **48** | (うち project 配下 2 件 = legacy redirect) |

### §0.1 認証画面 (auth route group — 未ログインアクセス可)

| 画面 | ルート | 目的 / 主な操作 | 権限 |
|---|---|---|---|
| ログイン | `/login` | 組織 ID (Tenant.slug) + メール + パスワードで認証。CWE-601 対策の callbackUrl sanitize。localStorage のテナント履歴 (LRU 5 件 / 90 日 expire) を候補表示。セットアップガイドリンク併設 | 全員 (未認証可) |
| MFA 検証 | `/login/mfa` | パスワード認証後の TOTP 6 桁検証。MFA 未有効 or 既検証なら callbackUrl へ自動 redirect。検証成功で JWT update → dashboard | password 認証済セッション保持者 |
| サインアップ | `/signup` | 公開セルフサインアップ。入力順は **組織情報 → 初期管理者 → プラン選択 → (Expert/Pro のみ) 請求先** (feat/signup-friction-reduction 2026-06-12: メールをプランより前に置き 3 層 eligibility をプラン選択へ反映)。LP から `?plan=expert` / `?plan=pro` で遷移するとプランが事前選択される (feat/signup-plan-preselect 2026-07-05: ホワイトリスト外の値は `beginner` にフォールバック。あくまで初期値の UX 便宜でありサーバ側 zod enum バリデーションが最終防御)。**組織 ID は入力欄なし**=サーバが数字連番を自動採番 (作成時に確定、衝突時リトライ)。採番値は成功画面と招待メールで本人へ案内。Beginner は請求先非表示・クレカ不要。honeypot + IP rate limit。送信後の成功画面は **自動採番された組織 ID + メール予告 (差出人 noreply@tasukiba.com / 件名「たすきば - アカウントの設定」/ 到着目安 1 分 / 24h 有効)** + 再送 + トラブルシュート。検証メール経由でパスワード設定 | 全員 (未認証可) |
| パスワード再設定 | `/reset-password` | 2 ステップ (verify → reset)。組織 slug + メールで本人確認 → reset token → 新パスワード設定。メールリンク経由の `?tenant=` 初期値取得 (ADR-0016) | 全員 (未認証可) |
| 初期パスワード設定 | `/setup-password` | 招待/検証メールリンクからの初回パスワード設定。super_admin のみ MFA 登録 (QR + TOTP) が必須、admin/general は即時有効化。完了後リカバリーコード表示 | 検証トークン保持者 |

### §0.2 プロジェクト系

| 画面 | ルート | 目的 / 主な操作 | 権限 | 詳細 |
|---|---|---|---|---|
| ダッシュボード入口 | `/` | `/projects` へ即時 redirect (実体なし) | 認証済 | — |
| プロジェクト一覧 | `/projects` | 全参加プロジェクトの一覧・検索・新規作成 | 全ロール (作成は admin/PM/TL) | §11.1 |
| プロジェクト詳細 | `/projects/[id]` | 概要 + タブハブ (WBS/ガント/リスク/課題/振り返り/見積/メンバー/参考)。各タブは client 遅延取得。`?tab=` `?suggestions=1` で deep link。非メンバー + 非 admin は `notFound()` | ProjectMember or admin | §11.2 |
| WBS / タスク | `/projects/[id]/tasks` | タスク階層管理・一括編集・複製・WBS export/import (sync) | ProjectMember | §11.4 |
| 進捗確認 (旧称: ガントチャート) | `/projects/[id]/gantt` | ガント表示 + 担当者別確認ボード (表示専用)。今日列を初期スクロール位置に | ProjectMember | §11.5 |
| 分析 (タブ) | `/projects/[id]` 分析タブ | 5 パネル: WBS 予実カーブ / 担当者別 週次消化工数 / 予定 vs 実績工数 / 作業負担 / 日次工数 (8h 上限ヒートマップ)。表示専用 | PM/PL + admin (closed 可) | §11.5b |
| リスク一覧 (タブ) | `/projects/[id]/risks` | プロジェクト配下リスク CRUD (type=risk) | ProjectMember | §11.7 |
| 課題一覧 (タブ) | `/projects/[id]/issues` | プロジェクト配下課題 CRUD (type=issue)。RisksClient を typeFilter='issue' で再利用 | ProjectMember | §11.7 |
| 振り返り (タブ) | `/projects/[id]/retrospectives` | プロジェクト振り返り作成・確定 | ProjectMember | §11.9 |
| 見積もり | `/projects/[id]/estimates` | 見積項目管理。**member/viewer は `notFound()` (404)**、API も 403 (PR #416) | admin / PM/TL のみ | §11.3 |
| (互換) ナレッジ | `/projects/[id]/knowledge` | **legacy redirect** → `/knowledge` (旧通知 deep link 互換、実体 UI なし) | — | — |
| (互換) ステークホルダ | `/projects/[id]/stakeholders` | **legacy redirect** → `/projects/[id]?tab=stakeholders` (旧通知 deep link 互換)。実体は詳細画面のタブ | — | — |

### §0.3 プロジェクト横断 (資産) 画面

5 画面 (リスク/課題/ナレッジ/振り返り/メモ) は統一レイアウト規約 (§20.4 / UI_PATTERNS §34) に準拠。詳細ダイアログは read-only。

| 画面 | ルート | 目的 | 権限 |
|---|---|---|---|
| 全リスク | `/risks` | テナント内 type=risk を横断表示。admin はモデレーション削除可 | 全ロール (閲覧は可視性準拠) |
| 全課題 | `/issues` | type=issue を横断表示 (AllRisksTable を typeFilter='issue' 再利用) | 全ロール |
| 全ナレッジ | `/knowledge` | visibility=public ナレッジを横断表示。N:M でプロジェクト紐付け | 全ロール |
| 全振り返り | `/retrospectives` | 横断振り返り表示。非メンバーには linkedProjects 名を null マスク | 全ロール |
| 全メモ | `/all-memos` | 全ユーザの public メモを横断表示 (read-only)。admin は他人 public のみ削除可、private は不可 | 全ロール |

### §0.4 個人・顧客・運用管理画面

| 画面 | ルート | 目的 | 権限 | 詳細 |
|---|---|---|---|---|
| マイタスク | `/my-tasks` | 自担当タスクを全プロジェクト横断で参照 (WBS/ガント UI 流用・参照専用、編集は各 WBS へ) | 全ロール (自分担当) | §11.6 |
| メモ (個人) | `/memos` | 自分のメモ (private/public) の CRUD。アカウントメニューから導線 | 認証済ユーザ | §21 |
| 顧客一覧 | `/customers` | 顧客 (Customer) マスタ一覧・新規・カスケード削除 | admin | §11.11b |
| 顧客詳細 | `/customers/[id]` | 顧客情報編集 + 紐付 active プロジェクト一覧 + カスケード削除 | admin | §11.11b |
| ユーザ管理 | `/admin/users` | システムロール / 利用状態 / ロック解除。自己ロール変更禁止、super_admin 選択肢非表示。DB 取得失敗時は空配列 + 警告バナーで描画継続 | admin + super_admin | §11.11 |
| 監査ログ | `/admin/audit-logs` | テナント内の業務データ操作 (作成/更新/削除) を新しい順に表示。列=日時/操作者/操作/対象/対象名。操作・対象・操作者・キーワードで絞り込み + 並び替え。表示件数 (100/300/1000/全件) 選択 + 100 行/ページのページネーション。リンク/ファイルの添付・削除は「どの画面 (リスク/ナレッジ等) で行われたか」を表示。詳細は [design/OBSERVABILITY.md §1.1](../design/OBSERVABILITY.md) | admin + super_admin | §11.13 |
| 権限変更履歴 | `/admin/role-changes` | システム/プロジェクトロール変更の監査・CSV 出力 | admin + super_admin | §11.12 |

### §0.5 設定・データ移行・ヘルプ

| 画面 | ルート | 目的 / 主な UI | 権限 | 詳細 |
|---|---|---|---|---|
| ユーザ個別設定 | `/settings` | アカウント情報 + 画面テーマ (10 種) + パスワード変更 + MFA + **他の端末からログアウト** の 5 セクション。**言語/TZ は個人設定になく、テナント設定 (admin) に集約済 (PR-1 #327)**。DB 取得失敗時はフォールバック描画 | 認証済ユーザ | §22.A / §23 / §24 / §25 |
| テナント設定 | `/settings/tenant` | プラン変更 (Expert↔Pro 即時 / Beginner ダウングレード禁止) + 月次予算上限 + 当月使用量 + DB/ファイルストレージ従量課金 + Stripe カード情報 + 縮退モード状態 | admin のみ (super_admin/general は redirect) | §0.6 |
| 請求履歴 (テナント) | `/settings/tenant/billing` | 自テナント直近 6 ヶ月の BillingHistory + 当月請求予定 + 支払期日 (監査 C-G6 解消) | admin のみ | — |
| CSV データ移行 | `/settings/tenant/migration-import` | 4 ステップ: ① 種類ごとに CSV 選択 → ② カラムマッピング (列 → 画面項目、既定値可) → ③ dry-run プレビュー (件数 + エラー + 想定課金) → ④ 取込結果。7 種 (顧客/プロジェクト/WBS/リスク課題/ナレッジ/振り返り)、新規作成のみ | admin のみ (general/super は redirect) | §0.6 |
| API 連携インポート (ベータ) | `/settings/tenant/api-import` | Notion/Backlog/kintone/Pleasanter/Google スプレッドシートに直接接続し 7 種を取込。接続→マッピング→プレビュー→取込。トークンは非保存 | admin のみ | §0.6 |
| ヘルプ・FAQ | `/help` | 一般 FAQ (accordion) + テナント管理者向け生成 AI/課金説明 (admin/super_admin のみ表示)。たすきフクロウ AI ヘルプチャット | 認証済ユーザ | — |
| 使い方ガイド | `/guide` | サインアップ直後の全体像 + ロール別やること + 用語。systemRole + ProjectMember.projectRole からユーザに合うセクションのみ表示 | 認証済ユーザ | — |

### §0.6 運営者専用画面 (super_admin / `/admin/super` 配下)

> **これらはすべて運営者専用 (super_admin)**。親 `layout.tsx` の super_admin guard + 一部は middleware Basic Auth で多層防御。顧客テナントの admin/general からは一切到達不可。Default テナント (= 運営者自身) は請求対象外として各画面で別枠表示される。

| 画面 | ルート | 目的 / 主な UI 要素 | 関連 service |
|---|---|---|---|
| 運営ダッシュボード | `/admin/super` | 全テナント横断サマリ (テナント数/ユーザ数/当月 API 呼出/合計課金) + Voyage 無料枠 / Anthropic 使用量 / Beginner 状況 / Netlify credits / DB 容量 / メール送信 / 休眠テナント / ストレージ TOP10 / DB・ファイルストレージ従量課金 alert。Suspense ストリーミング + 全テナント再集計ボタン。異常検知時は診断画面への赤バナー | super-admin / db-capacity / email-send-log / netlify-metrics / diagnostics |
| テナント一覧 | `/admin/super/tenants` | 顧客テナント一覧 (tenantSeq 昇順、ApiCallLog SUM 真値表示 + drift バッジ) + 新規払い出し導線。Default テナントは別セクション | super-admin / api-usage-recalc |
| 新規テナント払い出し | `/admin/super/tenants/new` | 顧客テナント発行 (請求先 + 初期 admin)。STRIPE_ENABLED で credit_card option を動的 disable | (TenantCreateForm) |
| テナント詳細 | `/admin/super/tenants/[id]` | entity 数 + 当月使用量 + 休眠日数 + 個別再集計 + 停止/削除ボタン。遷移時に当該テナントを即時再集計 | super-admin / tenant-storage / api-usage-recalc |
| テナント診断 | `/admin/super/tenants/[id]/diagnostics` | counter vs ApiCallLog SUM 整合性 + drift 修復ボタン + 直近 30 日日別 API + counter 書換 audit + 月次履歴整合性 | tenant-diagnostics |
| 使用量サマリ | `/admin/super/usage` | 全テナント横断使用量カード + プラン別分布 + CSV ダウンロード (当月=現在値/過去月=履歴) + 過去 6 ヶ月履歴テーブル | super-admin |
| 請求ダッシュボード | `/admin/super/billing` | 当月サマリ (請求総額/入金済/入金待ち/失敗) + 支払方法別内訳 + 過去 6 ヶ月推移 (月次詳細リンク) | billing-dashboard |
| 月次請求詳細 | `/admin/super/billing/[yearMonth]` | 指定月のテナント別 BillingHistory 一覧 + status/paymentMethod フィルタ + credit_card 行に Stripe Dashboard ディープリンク + 入金確認ボタン | billing-dashboard |
| 診断ダッシュボード | `/admin/super/diagnostics` | システム全体健全性を 1 画面集約: API drift / cron 健全性 / 縮退モード / メール失敗 / alert 空打ち。各セクションに修復導線 | diagnostics |
| cron 実行履歴 | `/admin/super/cron-history` | 直近 24h サマリ + 全 cron 動作概要 + 実行履歴 100 件 (status バッジ + duration + stale running 警告で Netlify 10s timeout 検知) | cron-history |
| メール送付失敗 | `/admin/super/email-failures` | 直近 24h の success=false な EmailSendLog 一覧 (type 別集計、recipient はハッシュ化/ドメイン部のみ)。`?hours=` `?limit=` 制御 | email-send-log |
| Stripe DLQ | `/admin/super/stripe-dlq` | Webhook DLQ/未処理 + Usage Record DLQ/未送信の一覧 + 再投入ボタン (retryCount リセット) | stripe-dlq |
| 周知バナー一覧 | `/admin/super/banners` | 全ユーザ共通の帯メッセージ (ADR-0036) の履歴一覧。状態バッジ (表示中/予約/終了/停止) + 編集/複製/取り下げ・再開/削除。1 本制約 (期間重複禁止) | system-banner |
| 周知バナー新規作成 | `/admin/super/banners/new` | メッセージ/緊急度(高赤・中黄・低青)/表示期間/有効を入力して作成。`?from=<id>` で複製 prefill。期間重複は 409 で弾く | system-banner |
| 周知バナー編集 | `/admin/super/banners/[id]/edit` | 既存バナーの内容・緊急度・期間・有効/無効を変更 | system-banner |

---

## §11. 主要画面仕様

## 11. 主要画面仕様

## 11.1 プロジェクト一覧画面

### 画面目的
全プロジェクトを一覧し、対象案件へ遷移する入口画面。

### 一覧項目
| 項目 | 内容 |
|---|---|
| プロジェクト名 | 案件名称 |
| 顧客名 | 顧客企業名 |
| 開発方式 | スクラッチ / PowerPlatform など |
| PM/TL | 主担当者 |
| ステータス | 企画中〜クローズ |
| 開始予定日 | 計画開始日 |
| 終了予定日 | 計画終了日 |
| 進捗概要 | 完了タスク数 / 全タスク数 など |
| 高優先度リスク件数 | 優先度高の未クローズ件数 |
| 更新日時 | 最終更新日時 |

### 検索・絞り込み項目
- キーワード
- 顧客名
- 開発方式
- ステータス
- PM/TL
- 開始予定日範囲
- 終了予定日範囲

### ボタン制御
| ボタン | システム管理者 | PM/TL | メンバー | 閲覧者 | 備考 |
|---|---:|---:|---:|---:|---|
| 新規プロジェクト作成 | ○ | ○ | × | × | 状態制約なし |
| 検索 | ○ | ○ | ○ | ○ | 常時可 |
| 条件クリア | ○ | ○ | ○ | ○ | 常時可 |
| 詳細を開く | ○ | ○ | ○ | ○ | 参加プロジェクトのみ |
| クローズ案件表示切替 | ○ | ○ | ○ | ○ | 常時可 |

## 11.2 プロジェクト詳細画面

### 画面目的
プロジェクト全体情報の集約。タブで各機能へ遷移するハブ画面。

### 表示項目
| 項目 | 内容 |
|---|---|
| プロジェクト名 | 案件名称 |
| 顧客名 | 顧客企業名 |
| 目的 | プロジェクト目的 |
| 背景 | 案件背景 |
| スコープ | 対象範囲 |
| スコープ外 | 対象外範囲 |
| 開発方式 | 開発方式 |
| 技術スタック | 主要技術 |
| PM/TL | 主担当 |
| メンバー数 | 参加人数 |
| 開始予定日 | 予定開始日 |
| 終了予定日 | 予定終了日 |
| ステータス | 現在状態 |
| 進捗概要 | タスク進捗サマリ |
| リスク概要 | 高優先度件数など |
| 実績概要 | 実績工数サマリ |
| 最終更新日時 | 更新日時 |

### 入力項目
| 項目 | 必須 | 備考 |
|---|---|---|
| プロジェクト名 | ○ | 100文字以内 |
| 顧客 | ○ | Customer マスタからの選択式 (PR #111-2: 自由入力を廃止、customer_id FK で管理) |
| 目的 | ○ | 2000文字以内 |
| 背景 | ○ | 2000文字以内 |
| スコープ | ○ | 2000文字以内 |
| スコープ外 | × | 2000文字以内 |
| 開発方式 | ○ | 選択式 |
| 対象業務領域 | × | 複数選択可 |
| 技術スタック | × | タグ形式 |
| 開始予定日 | ○ | 日付 |
| 終了予定日 | ○ | 日付 |
| PM/TL | ○ | ユーザ選択 |
| 備考 | × | 2000文字以内 |

### ボタン制御
| ボタン | 企画中 | 見積中 | 計画中 | 実行中 | クローズ | 実行可能ロール |
|---|---:|---:|---:|---:|---:|---|
| 編集開始 | ○ | ○ | △ | △ | × | システム管理者、PM/TL |
| 保存 | ○ | ○ | △ | △ | × | システム管理者、PM/TL |
| 状態変更 | ○ | ○ | ○ | ○ | △ | システム管理者、PM/TL |
| クローズ | × | × | × | △ | × | システム管理者、PM/TL |
| メンバー一覧表示 | ○ | ○ | ○ | ○ | ○ | 全ロール |
| メンバー管理へ | ○ | ○ | ○ | ○ | △ | システム管理者のみ |
| 履歴表示 | ○ | ○ | ○ | ○ | ○ | システム管理者、PM/TL |

## 11.3 見積もり管理画面

### 画面目的
見積もり項目・工数・入力根拠を記録し、カテゴリ別集計・Excel / Word エクスポートで提案書作成を支援する画面。

> **★2026-05-20 PR #416 で変更★ 認可の UI=API 一致原則**
> - 見積タブ・見積専用 page (`/projects/[id]/estimates`) は PM/TL + admin のみアクセス可
> - **API レベルでも member/viewer は 403** (UI 非表示と一致、curl 直叩き不可)
> - 見積専用 page は `!canEdit` (= member/viewer) の時 `notFound()` を呼び 404 を返す (リンク偽装防止)

### 一覧列 (v1.5.0 現在)
| 列 | 内容 |
|---|---|
| 行選択チェックボックス (v1.5.0) | 集計・エクスポート対象行を絞り込む。ヘッダーの全選択は全ページをまたいで一括選択/解除 |
| 見積項目名 | 作業/機能名 |
| 区分 | 工程/機能区分 (要件定義/設計/開発/試験等) |
| 入力モード (v1.2.0) | 手動登録 / 係数ベース (開発ツール・係数から自動計算) |
| 見積工数 | 数値 + 単位 (人時 h / 人日) |
| 備考 | 任意の補足コメント |
| ステータス | 未確定 / 確定 |
| 作成者 / 作成日時 | 登録者・登録日時 |
| 更新者 / 更新日時 | 最終更新者・日時 |
| アクション (canEdit のみ) | 確定ボタン (未確定行) / 削除ボタン (未確定行) |

### 行選択によるエクスポート範囲指定 (v1.5.0)
- **チェックなし (デフォルト)**: 全行がサマリ集計・Excel / Word エクスポートの対象
- **1件以上チェック**: チェックした行のみが対象
- 選択状態は全ページをまたいで保持 (ページ変更後も維持)
- CRUD 操作 (追加・確定・削除) 後は選択がリセットされる

### 入力項目 (v1.2.0 現在)

**共通 (手動登録 / 係数ベース)**
| 項目 | 必須 | 備考 |
|---|---|---|
| 見積項目名 | ○ | 100文字以内 |
| 区分 | ○ | 要件定義/設計/開発/試験等 (選択式) |
| 見積工数 | ○ | 数値 |
| 単位 | ○ | 人時 / 人日 |
| 備考 | × | 1000文字以内 |

**係数ベースモード追加項目 (ツールで見積もる)**
| 項目 | 必須 | 備考 |
|---|---|---|
| 開発ツール/方式 | ○ | 選択式 (スクラッチ等) |
| 基準時間 | ○ | 数値 (h) |
| 規模係数 | ○ | 数値倍率 |
| 難易度係数 | ○ | 数値倍率 |

### ボタン制御 (v1.2.0 現在)
| ボタン / 操作 | 企画中 | 見積中 | 計画中 | 実行中以降 | 実行可能ロール |
|---|---:|---:|---:|---:|---|
| 手動登録ボタン | △ | ○ | × | × | システム管理者、PM/TL |
| ツールで見積もるボタン | △ | ○ | × | × | システム管理者、PM/TL |
| 確定ボタン (未確定行) | × | ○ | △ | × | システム管理者、PM/TL |
| 削除ボタン (未確定行) | △ | ○ | △ | × | システム管理者、PM/TL |
| Excel エクスポート | ○ | ○ | ○ | ○ | システム管理者、PM/TL |
| Word エクスポート | ○ | ○ | ○ | ○ | システム管理者、PM/TL |

## 11.4 WBS / タスク管理画面

### 画面目的
タスクの階層構造、担当、予定、状態を管理する中核画面。

### 一覧項目
| 項目 | 内容 |
|---|---|
| WBS番号 | 階層番号 |
| タスク名 | タスク名称 |
| 親タスク | 上位タスク |
| 区分 | 工程区分 |
| 担当者 | 1名 |
| 開始予定日 | 日付 |
| 終了予定日 | 日付 |
| 実績開始日 | 日付（担当メンバーが入力） |
| 実績終了日 | 日付（担当メンバーが入力） |
| 予定工数 | 数値 |
| ステータス | 未着手/進行中/完了/保留 |
| 進捗率 | 0〜100% |
| 実績工数 | 累積実績 |
| 関連リスク件数 | 件数 |
| 更新日時 | 最終更新 |

### 入力項目
| 項目 | 必須 | 備考 |
|---|---|---|
| 親タスク | × | ルート可。WP 数が多い場合は検索欄で絞り込み可能（SearchableSelect）。作成・編集・一括複製ダイアログの 3 箇所に適用 |
| タスク名 | ○ | 100文字以内 |
| タスク内容 | × | 2000文字以内 |
| 区分 | ○ | 選択式 |
| 担当者 | ○ | 単一選択 |
| 開始予定日 | ○ | 日付（PM/TL が編集） |
| 終了予定日 | ○ | 日付（PM/TL が編集） |
| 実績開始日 | × | 日付（担当メンバーが編集） |
| 実績終了日 | × | 日付（担当メンバーが編集） |
| 予定工数 | ○ | 数値（PM/TL が編集） |
| 優先度 | × | 低/中/高 |
| ステータス | ○ | 未着手等（担当メンバーも編集可） |
| 進捗率 | ○ | 0〜100（担当メンバーも編集可） |
| 関連見積項目 | × | 1件以上可 |
| 関連ナレッジ | × | 複数可 |
| 関連リスク | × | 複数可 |
| 備考 | × | 1000文字以内 |

### ボタン制御
| ボタン | 計画中 | 実行中 | クローズ | 実行可能ロール |
|---|---:|---:|---:|---|
| タスク追加 | ○ | △ | × | システム管理者、PM/TL |
| 子タスク追加 | ○ | △ | × | システム管理者、PM/TL |
| 編集 | ○ | △ | × | システム管理者、PM/TL |
| 保存 | ○ | △ | × | システム管理者、PM/TL |
| 担当変更 | ○ | △ | × | システム管理者、PM/TL |
| 日程変更 | ○ | △ | × | システム管理者、PM/TL |
| 予定工数変更 | ○ | △ | × | システム管理者、PM/TL |
| 論理削除 | ○ | △ | × | システム管理者、PM/TL |
| 一括削除 | ○ | △ | × | システム管理者、PM/TL |
| 一括編集（計画系: 担当者・優先度・計画日程・予定工数） | ○ | △ | × | システム管理者、PM/TL |
| 一括実績更新（実績系: ステータス・進捗率・実績日程） (PR #88 改訂) | 自分担当タスクのみ○ | 自分担当タスクのみ○ | 自分担当タスクのみ○ | 担当者本人のみ (役割問わず) |
| WBSテンプレートエクスポート | ○ | △ | × | システム管理者、PM/TL |
| WBSテンプレートインポート | ○ | △ | × | システム管理者、PM/TL |
| **WBS エクスポート (新形式: ID + 進捗列込み)** | ○ | △ | × | システム管理者、PM/TL (feat/wbs-overwrite-import) |
| **WBS 上書きインポート (Sync by ID)** | ○ | △ | × | システム管理者、PM/TL (feat/wbs-overwrite-import) |
| **WBS 上書き dry-run プレビュー** | ○ | △ | × | システム管理者、PM/TL (feat/wbs-overwrite-import) |

#### WBS テンプレート仕様 (旧フロー: 別プロジェクトへの雛形流用)
- 形式: CSV（レベル列と行順序で階層構造を表現、Excel で編集可能）
- CSV列: レベル, 種別(WP/ACT), 名称, WBS番号, 予定開始日, 予定終了日, 見積工数, 優先度, マイルストーン, 備考
- エクスポート対象: チェックボックスで選択したタスク（未選択時は全タスク）
- エクスポート時: 担当者はリセット（テンプレートとして再利用可能）、BOM付きUTF-8でExcel対応
- インポート時: 進捗・ステータスは初期状態（`not_started` / `0%`）で作成
- 上限: 500件/テンプレート
- インポートバリデーション: 親参照整合性（存在しない親の検出）、tempId 重複、循環参照、親種別チェック
- エラー時: トランザクションにより全タスクがロールバック（不正データは残らない）

#### WBS 上書きインポート仕様 (feat/wbs-overwrite-import / PR #420 で UX 全面改修)

**目的**: 同一プロジェクト内の WBS を 「export → Excel 編集 → re-import」の往復編集サイクルで管理可能にする。旧テンプレート流用フローとは別系統で並走する。

**CSV フォーマット (T-19 で 7 列に削減)**:

| # | 列名 | 必須/任意 | 区分 | import 動作 |
|---|---|---|---|---|
| 1 | ID | 任意 | 突合 | UUID。値あり=既存と突合し UPDATE、空欄=新規作成 |
| 2 | 種別 | 必須 | 階層 | WP / ACT。WP↔ACT 切替は dry-run でエラー扱い |
| 3 | 名称 | 必須 | 計画 | UPDATE 反映、最大 100 文字 (VARCHAR(100)) |
| 4 | レベル | 必須 | 階層 | 1〜N、行順序と組合せで階層を再構築。**親は level スタックから推論** (= 同一親配下の判定に使用) |
| 5 | 予定開始日 | 任意 | 計画 | UPDATE 反映 (ACT のみ。WP は子から自動算出のため CSV 側を無視) |
| 6 | 予定終了日 | 任意 | 計画 | UPDATE 反映 (ACT のみ) |
| 7 | 予定工数 | 任意 | 計画 | UPDATE 反映 (ACT のみ) |

担当者 / 優先度 / マイルストーン / 備考 / WBS 番号 / 進捗系列は CSV 経由で扱わない (CSV の用途を「計画調整」に絞り、それ以外は UI 個別編集に集約)。BOM 付き UTF-8 で出力・受入 (Excel 対応)。

**ヘッダー検証** (PR #420 [A3]): 列名・列順・列数 (≥4) を厳格チェック。不一致は dry-run の `globalErrors` でブロッカー化。旧実装は silent skip で原因不明エラーになっていた問題を解消。

**エクスポート仕様 (新形式)**:
- エンドポイント: 既存 `POST /api/projects/[id]/tasks/export` を拡張 (後方互換維持のため `mode='sync'` パラメータで新形式を返す)
- 担当者・進捗・実績はリセットせず DB 値そのまま出力
- 画面上「IDを表示」トグル ON 時の表示と整合する

**インポートフロー (2 ステップ)**:

1. **dry-run プレビュー**: `POST /api/projects/[id]/tasks/sync-import?dryRun=1`
   - レスポンスにサマリ ({ added, updated, removed, blockedErrors, warnings }) と行ごとの差分を返す
   - 削除候補のうち進捗を持つタスク (progressRate > 0 or actualStartDate != null) は赤エラー扱い
   - DB は変更しない

2. **本実行**: `POST /api/projects/[id]/tasks/sync-import` (dryRun なし)
   - body に `removeMode: 'keep' | 'warn' | 'delete'` を含める
   - 成功 → import 件数サマリを返す
   - 失敗 → インポート前の状態に完全復元 (audit_log の beforeValue から)

**画面操作** (PR #420 で UX 全面改修):
- タスク一覧画面に「IDを表示」トグルボタンを追加 (既定 OFF、ON で先頭に ID 列表示)
- 「WBSをエクスポート」「WBSを上書きインポート」ボタンを別途追加 (PM/TL + admin のみ表示)
- 上書きインポートは dry-run プレビューダイアログを必ず経由する 2 ステップ UX

**ダイアログ UI (PR #420 で追加された UX 機能)**:
- **CSV テンプレート DL ボタン** [W1] — Step 1 でクリック 1 回でヘッダー + サンプル 3 行を BOM 付き UTF-8 でダウンロード
- **D&D アップロード対応** [B4] — ファイル選択 input をクリックする以外に、ドロップゾーンに CSV を D&D 可。非 CSV は明示拒否
- **ファイル選択時の自動 dry-run** [W2] — 「プレビュー生成」ボタン廃止。選択直後にプレビュー画面へ遷移
- **エラー / 警告 / 全件 フィルタトグル** [B1] — 大量行でも問題行を見つけやすくする
- **REMOVE_CANDIDATE 行のテーブル内ハイライト** [B5] — 進捗あり=赤、進捗なし=黄
- **削除候補アコーディオン** — 「削除候補の扱い」ラジオで「削除実行」を選んだ時のみラジオ直下に出現。デフォルト閉じ、クリックで展開。削除モードが変わると閉じ状態にリセット
- **削除モード=delete 実行直前の確認ダイアログ** [W3] — 削除対象タスク名を列挙してユーザに確認させる
- **エラーメッセージのガイダンス化** [B2] — 技術文言ではなく業務文言 + 修正ヒント (例: 「親レベル N-1 が直前にありません」→ 「親 WP を CSV の上行に配置してください」)
- **サマリ grid / ボタンの sm: breakpoint レスポンシブ対応** [B7] — モバイル幅でも操作可能

**エラー分類**:

| 重大度 | 条件 | 動作 |
|---|---|---|
| ブロッカー | ヘッダー不正 (列名・列順) / 必須列空 / 種別不正 / 親不在 / WP↔ACT 切替 / ID が DB に存在しない or 別プロジェクト / CSV 内 ID 重複 / 並行編集検出 (OCC) / 進捗あり削除候補 (削除モード時) | dry-run でエラー、実行不可 |
| 警告 | **同一親配下の同名タスク (CSV 内重複 / 既存と同名、ID 空欄で新規作成)** / 削除候補が存在する (warn モード) / 大量取込で処理に時間がかかる可能性 (globalWarnings) | 実行可、確認後 OK |
| 情報 | 追加 / 更新 / 名称変更 / 親変更 | 表示のみ |

> **ADR-0032 (2026-06-04)**: 「同一親配下の同名タスク」「ID 不一致だが同名 (誤コピー検知)」はブロッカー → **警告**に変更。DB 部分 UNIQUE 制約も撤廃。タスク突合は ID (UUID) のみで行うため同名でも機能は壊れず、週内に繰り返す学習タスク等の同名を許容する。

**バリデーション + ロールバック**:
- PgBouncer 制約で `prisma.$transaction` 不可のため、開始時に対象タスクの完全コピーを `audit_log.beforeValue` に保存
- エラー発生時:
  1. それまで作成済の Task は物理削除
  2. UPDATE 済の Task は beforeValue から完全列復元
  3. DELETE 済の Task (deletedAt セット済) は `deletedAt: null` に戻す
- 完了後: `audit_log` を 1 件追加 (action='SYNC_IMPORT', afterValue=summary)

**上限・性能 (ADR-0032 で改訂)**:
- 業務上のハード行数上限は撤廃 (旧 500 件)。目安 (`TASK_SYNC_IMPORT_WARN_ROWS`=300) 超は「処理に時間がかかる場合がある」警告のみでブロックしない。DoS 安全弁として `TASK_SYNC_IMPORT_MAX_ROWS`=2000 を route 層に残す
- 本実行 (`applySyncImport`) はバッチ化:
  - 新規行は app 側 UUID 採番で level 昇順に `createMany` (ADR-0032)
  - **既存行の UPDATE は `$transaction` 配列形で 100 件ごとにチャンク一括** (ADR-0037。旧実装は 1 行ずつ `await update` の逐次往復で、更新主体の WBS が Netlify 10 秒上限を超え 504 になっていた)
  - **WP 集計は `recalculateAllProjectWps` を「全タスク 1 fetch + メモリ集計 (深度降順) + 変更 WP のみ `$transaction` 一括 update」に** (ADR-0037。旧実装は WP ごとに findUnique+update の O(WP) 逐次往復で、これ単体でも 10 秒に迫っていた)。往復は WBS のサイズに依存しないほぼ定数になる
  - 削除候補は `updateMany` で一括論理削除
  - 最終的な格納値・監査内容は旧逐次実装と同一

**並行編集検出 (OCC, PR #420 [C2])**:
- dry-run 時点で `project` 配下 task の最大 `updatedAt` を `snapshotAt` として返す
- 本実行は client が `x-import-snapshot-at` header に snapshotAt を添えて送信
- server 側で再取得した snapshotAt と比較し、不一致なら `IMPORT_CONCURRENT_EDIT` (HTTP 409) で中断 (= 他ユーザが dry-run 後にタスクを更新したことを検出)
- 旧 UI (header 未送信) は best-effort で OCC スキップ

**DB レベル制約 (PR #420 [C3] → ADR-0032 で撤廃)**:
- ~~tasks に `(project_id, parent_task_id, name) WHERE deleted_at IS NULL` の部分 UNIQUE インデックス~~ は **ADR-0032 (2026-06-04) で撤廃** (migration `20260610_drop_tasks_unique_parent_name`)
- アプリ層の名称一意性ガード (`assertTaskNameUniqueInParent` / sync-import の同名ブロック) も撤去
- 撤廃理由: タスク突合は ID (UUID) のみで行い名前で照合しないため同名でも機能は壊れず、週内に繰り返す学習タスク等の同名は業務上正当。旧実装は「プレビュー警告のみ → 本実行で P2002 → 500」というドリフトを起こしていた

**スコープ外 (将来 PR 候補)**:
- 列名のヘッダーゆらぎ吸収
- プレビュー画面でのインライン編集 [B3 defer]
- 部分成功・警告のみ強制実行オプション [B6 defer]
- インポート履歴 (直近 5 件) のダイアログ内表示 [W4 defer]
- 進捗系列を書き戻し可能にする上級モード

#### WBS タスク一括複製仕様 (PR #420 で新規追加)

**目的**: WBS 一覧でチェック ON のタスクを **指定の親 WP (または ルート) 配下に階層保持で複製** する。インポート手順を経ずに画面上で複数作成する導線。

**画面操作**:
- WBS 一覧でタスクにチェックを入れた状態で bulk action bar 内「**複製**」ボタンが活性化 (PM/TL + admin のみ表示、一括編集と同等権限)
- ボタンクリックで親選択ダイアログを開く
- 「複製先の親 WP」プルダウン (`ルート / プロジェクト直下` + 既存全 WP)
- 確認用に動作仕様を 3 行表示
- 「複製を実行」ボタンで API 呼び出し

**動作仕様 (確定 4 項目)**:

| 項目 | 動作 |
|---|---|
| **名称衝突** | 自動リネーム "(コピー)", "(コピー 2)" suffix。VARCHAR(100) を超えないよう base を truncate |
| **階層保持** | 選択範囲内の親子関係は維持。選択外を親とするタスクは target parent 直下に移動 |
| **計画情報のコピー** | name / type / description / category / assignee / planned 開始終了日 / plannedEffort / priority / isMilestone / notes |
| **実績情報のリセット** | `status='not_started'` / `progressRate=0` / `actualStartDate=null` / `actualEndDate=null` / TaskProgressLog 非複製 |

**バリデーション (ブロッカー)**:
- `PROJECT_NOT_FOUND`: テナント越境チェック
- `TASKS_OUT_OF_RANGE`: 0 件 / 101 件以上
- `TASKS_NOT_FOUND:<ids>`: 指定 ID が当該 project に存在しない (= 他 project の ID も同扱いで enumeration 防止)
- `TARGET_PARENT_NOT_FOUND` / `TARGET_PARENT_NOT_WP`: target parent が削除済 or ACT
- `ACT_CANNOT_BE_ROOT:<names>`: ACT を root or ACT の配下に置こうとした

**集計再計算**:
- 新規作成 WP 自身も `recalculateAncestorsPublic` の対象 (= 新規 ACT 子の plannedEffort が WP に rollup される)
- 加えて target parent も再計算 (= 配下が増えたため)

**監査ログ**:
- `audit_logs` に 1 件追加 (action=`BULK_DUPLICATE`, entityType=`task`, entityId=projectId, afterValue=`{ targetParentId, sourceTaskIds, added, renamedCount, createdIds }`)

**上限**: 100 件 (既存 bulk-edit と揃える)

**スコープ外 (将来 PR 候補)**:
- リネーム接尾辞のカスタマイズ (現在 "(コピー)" 固定)
- 複製対象に進捗ログまで含める高度モード
| 進捗更新 | △ | ○ | △ | 担当者本人のみ (役割問わず、PR #88 改訂) |
| 実績入力 | △ | ○ | △ | 担当者本人のみ (役割問わず、PR #88 改訂) |
| コメント入力 | △ | ○ | △ | システム管理者、PM/TL、担当メンバー |
| リスク起票 | ○ | ○ | △ | システム管理者、PM/TL、メンバー |
| 詳細表示 | ○ | ○ | ○ | 全ロール |

#### WBS カンバンビュー (v1.4.0)

**目的**: タスクの担当者別・日付別の工数配分をひと目で把握する。ツリービューと排他切替で共存。

**グリッドレイアウト**:
- 列 = 稼働日 (月〜金、土日除く)
- 行 = 担当者 (担当なしは最下部)
- 未スケジュール行 = 日付未設定タスクをまとめて表示

**付箋 (TaskChip) 高さ設計**:
- 8h = 200px (FULL_HEIGHT_PX)
- 最低高さ = 32px (MIN_HEIGHT_PX)
- 計算式: `max(32, round((dailyEffort / 8) * 200))` px
- 日次工数 = 総予定工数 ÷ 稼働日数 (C方式)

**セルハイライト**:
- 1日の工数合計 ≥ 7h → 黄色背景 (注意)
- 1日の工数合計 ≥ 8h → 赤背景 (過負荷)

**ホバーツールチップ**: WP名 / ACT名 / 担当者 / ステータス / 期間 / 総予定工数 / 日次工数(推定)

**ビュー切替**:
- フィルター行末尾の「ツリービュー / カンバンビュー」トグルボタン
- 選択状態はセッション中永続 (`wbs:<projectId>:view-mode`)
- 両ビューで同じ編集ダイアログ・一括操作バーを共有

**v1.4.0 非対応 (将来 PR 候補)**:
- ドラッグ&ドロップで日付変更
- 祝祭日の除外
- モバイル対応 (PC のみ表示)

## 11.5 進捗確認画面 (旧称: ガントチャート画面、v1.3.0 でタブ名変更)

> **★実装ミラー★** 本節は `src/app/(dashboard)/projects/[projectId]/gantt/gantt-client.tsx` (`GanttClient`) に同期。プロジェクト詳細の「進捗確認」タブ (内部 tab key は後方互換のため `gantt` を維持。URL `?tab=gantt` deep link は変更なし) として表示される。

### 画面目的
プロジェクトの進捗を確認する画面。スケジュールを時系列で可視化する **ガント表示** と、担当者ごとに直近のタスク状況を一覧する **担当者別確認** (v1.3.0 追加) を、画面上部のチップで個別に表示/非表示切替できる (両方同時表示も可。analysis タブの表示グラフ選択チップと同方式)。突発タスクの差し込みが多いアジャイル的な進め方でも、PM が担当者ごとに「終わったこと/やっていること/これからやること」を一目で把握し、ヒアリングの準備に使えることを意図する。

### 表示切替 (チップ、v1.3.0 追加)
- 「ガント表示」「担当者別確認」の 2 チップで個別に ON/OFF 切替可能 (排他ではない)。初期表示は両方 ON。
- 選択状態は `sessionStorage` に永続化 (`gantt:{projectId}:visible-views`)。

### 11.5.1 ガント表示
#### 表示仕様
- タイムラインは**日単位**で表示（月ヘッダー + 日付/曜日ヘッダーの2段構成）
- 週末（土日）はグレー背景、当日は青背景でハイライト
- **日本の祝日** (元日・建国記念の日・こどもの日・敬老の日 等) は土日と同じグレー背景扱い、日付セルの `title` 属性に祝日名を出力 (マウスオーバーで祝日名表示) — PR #125 で追加
- 1日あたり32pxの固定幅でスクロール可能
- 親ワークパッケージ名をタスク名の上に小さく表示
- **初期表示位置 (feat/gantt-initial-scroll-and-locale 2026-05-29)**: 画面マウント時に **「今日」の日付列が左端** になるよう scrollLeft を `dayOffset(minDate, today) * DAY_WIDTH` で初期化。これにより毎回手動で今日にスクロールする手間を撲滅し、進行中タスクの状況を即座に確認できる
- **「今日」の判定**: server-side で `getTenantTodayString(new Date(), session.user.timezone)` により tenant TZ ベースで算出し、client に props で渡す (UTC 起点で日付がずれる罠を排除)
- **表示期間の保証**: minDate / totalDays 計算に today を必ず含めるため、全タスクが過去 or 未来のみのケースでも今日マーカーが表示レンジに必ず収まる
- **月ヘッダの表記**: `Intl.DateTimeFormat(tenantLocale, { timeZone: tenantTimeZone, year: 'numeric', month: 'long' })` で locale 依存に整形 (ja-JP → "2026年5月"、en-US → "May 2026")

#### 一覧項目
| 項目 | 内容 |
|---|---|
| 親WP名 | 所属するワークパッケージ名（小テキスト） |
| タスク名 | 表示対象タスク |
| 担当者 | 担当 |
| 開始予定日 | 日付 |
| 終了予定日 | 日付 |
| 進捗率 | 0〜100% |
| ステータス | 未着手等 |
| マイルストーン | 該当時のみ |
| 遅延フラグ | 遅延判定 |

### 11.5.2 担当者別確認ボード (v1.3.0 追加)

#### 画面目的
担当者ごとに「タスク全量」を **終わった / やっている / これから / 保留** の 4 列に分類したカードで一覧する。WBS で定義したタスクに加え、突発的に差し込まれたタスク (WP に属さない ACT) も同じ集計に含まれる。

#### 対象データ
- ガント表示と同じ tree データ (`tasksData.tree` 経由) を再利用。**新規 API は追加しない**。
- `type='activity'` (担当者を持つ実タスク) のみが対象。WP は対象外。
- ガント表示の **担当者フィルタを共有** する (選択中の担当者のみカード表示)。**状況フィルタは適用しない** (ボード自身が status × 日付で分類するため)。

#### ウィンドウ
- 「今日」を基準に **前後 3 日** (`BOARD_WINDOW_DAYS = 3`、calendar day、土日祝を区別しない)。

#### 分類ロジック (status × 日付)
| 列 | 対象 | 条件 |
|---|---|---|
| 終わった | `status='completed'` | 実績完了日 (無ければ予定完了日) がウィンドウ内 |
| やっている | `status='in_progress'` | ウィンドウに関わらず常に表示 (現在進行中であることが本質のため) |
| これから | `status='not_started'` | 予定開始日が未設定、またはウィンドウ終端以前 (= 既に開始予定日を過ぎている着手遅延タスクも含む) |
| 保留 | `status='on_hold'` | ウィンドウに関わらず常に表示 (一時停止中であること自体がヒアリング対象) |

#### 異常 (遅延) 強調
- 「やっている」「これから」列のタスクのうち、`status` が完了・保留以外で **予定終了日が「今日」を過ぎている** ものは、ガント表示の遅延バー (`isDelayed`) と同じ定義で「遅延」バッジを表示する。
- 「終わった」「保留」列には遅延バッジを付けない (保留自体が確認対象であることを列で表現済み)。

#### 表示形式
- **担当者カードのカンバン風**: 1 担当者 = 1 カード、カード内に上記 4 列。タスクが 0 件の列は「なし」と表示。
- 該当タスクが 1 件も無い担当者のカードは表示しない。対象担当者が 0 名の場合は空状態メッセージを表示。
- 担当者名 (カードの並び順) は tenant locale でソート。未アサインタスクは「（未アサイン）」カードに集約。

#### 入力項目
なし（表示専用。タスクの編集は WBS / タスク画面から行う）

### 入力項目
なし（MVPでは表示専用）

### ボタン制御
| ボタン | 実行可能ロール | 状態制約 |
|---|---|---|
| 表示切替 (ガント表示/担当者別確認チップ) | 全ロール | 常時可 |
| 表示期間切替（日/週/月） | 全ロール | 常時可 |
| 担当者絞り込み | 全ロール | 常時可 |
| ステータス絞り込み | 全ロール | 常時可 (ガント表示のみに影響) |
| タスク詳細へ遷移 | 全ロール | 常時可 |
| ガント直接編集 | なし | 常に不可 |

## 11.5b 分析タブ (WBS 予実カーブ) — v1.2.0

> **★実装ミラー★** 本節は `src/app/(dashboard)/projects/[projectId]/analysis/` (analysis-client.tsx / analysis-panels.ts / analysis-period.ts) / `src/services/analytics.service.ts` (getWbsCompletionCurve / getAssigneeWeeklyEffort / getAssigneeEffortVariance / getAssigneeWorkload / getAssigneeDailyCapacity + AnalyticsRange) / `src/lib/analytics-range.ts` / `src/components/charts/` (time-series-chart / stacked-bar-chart / grouped-bar-chart / heatmap-table) に同期。ADR-0038 参照。

### 画面目的
プロジェクト詳細の **分析** タブ (タブ並び: 「ナレッジ一覧」と「参考」の間)。PM/PL が「完了に向けた現在地」と「生産性 (消化ペース)」をグラフで把握する。パネルを縦に並べる構成で、現状は 5 枚。**表示名は「何を確認できるか」が分かる短い名前**にしている (本節は概念名で記述。表示名↔概念の対応は下表):

| # | 表示名 (UI チップ・i18n title) | 概念 (本節での呼称) |
|---|---|---|
| 1 | 進捗の遅れ・先行 | WBS 予実カーブ |
| 2 | 消化ペースと効率 | 担当者別 週次消化工数 |
| 3 | 見積の精度（予実差） | 担当者別 予定 vs 実績 工数 |
| 4 | 作業量の偏り | 担当者別 作業負担 |
| 5 | 日別の負荷（8h超） | 担当者別 日次工数 (1 日 8h 上限チェック) |

### ツールバー (表示グラフの選択 + 対象期間) — v1.2.0 追補
- タブ最上部に sticky なツールバーを置く (`analysis-client.tsx`)。
- **表示グラフ (複数選択チップ)**: 5 パネルのタイトルをチップ (button) で並べ、ON/OFF を切替。**OFF のパネルは fetch もしない (遅延)**。**初期表示は予実カーブ 1 枚のみ** (`DEFAULT_VISIBLE=['wbs-completion']`。縦並びの圧迫を避ける)。全 OFF のときは「表示するグラフを選んでください」の空状態。
- **対象期間 (プリセット + カスタム)**: `全期間 / 直近1ヶ月 / 直近3ヶ月 / 直近6ヶ月 / カスタム(from–to)`。期間は**パネルの性質ごとに向きが異なる** (`analysis-period.ts#resolveRanges` + パネルの `rangeKind`):
  - **過去向き (`past`)**: ①予実カーブ / ②週次消化工数 / ③予実差。`[today-Nヶ月, today]` (カスタムは from–to) で対象を絞る。①は横軸クリップ (累積値は維持)、②③は対象タスクを絞り**効率・合計・順位を再集計** (サーバ側)。
  - **未来向き (`future`)**: ⑤日次工数。起点は常に本日、`to` (= today+Nヶ月 / カスタム to) で未来の終端を絞る。`to` が本日より前なら空。プリセット時はその月数を**今後側**に適用。注記「対象期間は今後側に適用」を表示。
  - **影響なし (`none`)**: ④作業負担。現在の残作業のスナップショットのため期間指定を反映しない。期間指定中は注記を表示。
- **永続化**: 表示グラフ・期間は `sessionStorage` に**ユーザ ID でキーを分離** (`analysis-visible:{userId}` / `analysis-period:{userId}`、`useSessionState` 流用) して保存。別ユーザに引き継がれない (越境防御。chat-history-storage と同方針)。
- **API への受け渡し**: 各パネルは `?from=YYYY-MM-DD&to=YYYY-MM-DD` でサービスに渡す (`parseAnalyticsRange` が形式不正を無視)。④ は range を送らない。

### 表示制御
- **表示ロール**: `systemRole==='admin'` または プロジェクト `projectRole==='pm_tl'` のみ (member / viewer には非表示)。API も `analytics:read` で二重防御 (非該当は 403、非メンバーは 404)。
- **closed プロジェクトでも表示**: 読み取り専用のため、完了案件の振り返り分析に利用できる (`isReadOnlyByStatus` に依存しない)。
- **遅延ロード**: `dynamic()` import + 自前 fetch (SuggestionsPanel と同型。`LazyTabContent` には乗せない)。

### パネル1 グラフ仕様 (WBS 予実カーブ・折れ線)
- 縦軸 = 着手割合 (0〜100%、10% 刻み)。分母 = ACT (type='activity', 削除除く) の総数。WP は分母に含めない。
- 横軸 = 日次。最小予定開始日 〜 max(最大予定完了日, 最大実績完了日, 本日)。
- **予定線 (実線)**: 各日 d で「予定完了日 ≤ d」の ACT 累積件数 ÷ 総数。最大予定完了日で 100%。
- **実績線 (点線)**: 各日 d で「status='completed' かつ 実績完了日 ≤ d」の ACT 累積件数 ÷ 総数。**本日より後は描かない (null)**。完了日に丸ごと計上 (按分しない)。
- **本日マーカー**: 本日位置の縦線 + 上部に「本日時点 予定◯% / 実績◯% (遅れ◯%)」サマリ。
- データが無い (ACT 0 件 / 実績未入力) 場合は空状態メッセージを表示。

### パネル2 グラフ仕様 (担当者別 週次消化工数・積み上げ棒 + 工数効率)
- 縦軸 = 実績工数 (人時、自動スケール)。横軸 = 週次 (月曜始まり、最初に完了した週 〜 max(本日週, 最後の完了週)。完了の無い中間週も 0 で連続表示)。
- 1 本の棒 = その週に完了した ACT (status='completed' かつ実績完了日あり) の **実績工数 (Task.actualEffort) を担当者別に積み上げ**。件数でなく工数で測ることでタスクの大小を吸収する。
- 担当者は実績工数の **上位 8 人を個別色**、残りは「その他」(グレー) に集約。担当者なしは「未割当」。実工数未入力 (null) の完了 ACT は棒に乗らない。
- **工数効率サマリ** (パネル上部): 工数効率 = Σ予定工数 ÷ Σ実績工数 (完了 + 実工数入力済の ACT のみ)。>1 効率的 / <1 想定超過。実工数が無ければ「算出できません」。EVM の CPI 相当で、絶対的生産性ではない点に留意 (docs/FAQ に明記)。
- ツールチップに各担当者の実績工数 + 合計を表示。最新の週は途中 (部分集計) になりうる。
- 本日週はテナント TZ で判定 (パネル1 の本日と整合)。
- 入力元: 実績工数は WBS の実績入力 (ACT 編集ダイアログの実績セクション) で担当者が入力する (§11.4)。

### パネル3 グラフ仕様 (担当者別 予定 vs 実績 工数・グループ棒)
- 横軸 = 担当者。各担当者に **予定工数バーと実績工数バーを横並び** (積み上げない) で描く。縦軸 = 工数 (人時、自動スケール)。
- 母数 = status='completed' かつ実績完了日あり かつ **実績工数 > 0** の ACT (予定・実績の両方が揃うタスクのみで公平に比較)。担当者ごとに plannedEffort / actualEffort を SUM。
- 担当者は実績工数の **上位 8 人を個別**、残りは「その他」に集約。担当者なしは「未割当」。
- ツールチップに 予定 / 実績 と **差 (実績 − 予定。＋超過 / −短縮)** を表示。
- 目的: PM/PL の予定工数と担当者の実績工数の差を担当者ごとに把握し、見積もり・タスク割り振りの参考にする。時系列でないため本日 (TZ) 判定は不要。

### パネル4 グラフ仕様 (担当者別 作業負担・状態別積み上げ棒 + モード切替)
- 横軸 = 担当者。1 本 = **未完了 ACT (not_started / in_progress) の予定工数**を状態別に積み上げ。縦軸 = 残工数 (人時、自動スケール)。完了タスクは負担に含めない。**保留 (on_hold) も集計に含めない** (ユーザ要件 2026-06-15。一時停止中で現在の負担とは言えないため)。
- **モード切替 (パネル右上のセグメント)**: 「割り振り量」= 予定工数そのもの / 「個人ペース補正」= 各担当者の **実績÷予定比** (完了+実工数入力済の履歴から算出。履歴なしは ×1) を掛けた予想残工数。補正は各状態セグメントに一律適用。
- 担当者は (選択モードの) 作業負担の **上位 8 人を個別**、残りは「その他」に集約。担当者なしは「未割当」。
- 要約ヘッダー (モード別): 平均 / 最も重い (担当者・値) / 最も軽い (担当者・値)。平均と比べて誰が重い/軽いかを把握。
- 目的: できる人への偏りを防ぎ、負荷分散・割り振りの材料にする。**限界**: 予定工数は見積で person-neutral、補正もスピード差を織り込むが見積誤差は残る。**個人評価には使わない** (説明文・FAQ・ガイドに明記)。未完了タスクが 0 件の担当者はタスク由来集計のため一覧に現れない。

### パネル5 グラフ仕様 (担当者別 日次工数・1 日 8h 上限チェック・ヒートマップ)
- **形式 = ヒートマップ表** (折れ線/棒とは別形式)。行 = 担当者 (1 列目は横スクロール時も固定)、列 = 日付 (本日以降)、セル = その日の予定工数 (人時)。セルは閾値レベルで色分け: **≤7h=無色 (OK) / >7h=黄 (警告) / >8h=赤 (超過)** (`classifyWorkloadLevel`、境界 7.0 は OK・8.0 は警告)。割当の無い日は空セル。下部に凡例。
- 対象 = **未完了 ACT (not_started / in_progress)**。完了・**保留 (on_hold) は除外**。`assigneeId` / 予定開始日 / 予定完了日 / 正の予定工数が揃う ACT のみ。
- 按分 = 各タスクの予定工数を予定期間 (予定開始日〜予定完了日, inclusive) の日数で **均等按分**し、担当者×日付で合算 (WBS 編集ダイアログの日次工数プレビュー `getAssigneeDailyWorkload` と同ロジック)。
- 横軸 = **本日 〜 未完了タスクの最大予定完了日**。本日より前の按分日は表示しない (過去の負荷は対象外)。本日はテナント TZ で判定。全タスクが過去の担当者は一覧に現れない。
- 並び順 = **8h 超 (超過) の日数 → 7h 超 (警告) の日数 → 最大日次工数** の降順 (逼迫している担当者ほど上)。担当者なしは「未割当」。
- 目的: 特定日への作業集中 (山積み) を早期に発見し、予定の前倒し/後ろ倒し・割り振り直しなど負荷の平準化を検討する。パネル4 (作業負担の総量比較) と役割を分け、本パネルは「いつ」に焦点を当てる。
- **限界**: 均等按分は概算で実際の作業配分 (前倒し/後ろ倒し) は反映しない。土日祝も 1 日として按分する (営業日按分は将来拡張)。**個人評価には使わない** (説明文・FAQ・ガイドに明記)。

### 汎用基盤
分析タブは 3 層 (サービス=ドメイン数値 / パネルレジストリ=表示組み立て / 共通チャート部品) で構成。チャート部品は `AnalysisChart` 契約のみを知り、`kind: 'line' | 'stacked-bar' | 'grouped-bar'` で `TimeSeriesChart` / `StackedBarChart` / `GroupedBarChart` を出し分ける。ヒートマップは別契約 `AnalysisHeatmap` を持ち `HeatmapTable` が描く (パネル5)。`AnalysisRenderData` は `chart` か `heatmap` のいずれかを持ち、`AnalysisPanelCard` が出し分ける。パネルは任意で `modes` (切替) を持て、`AnalysisPanelCard` がセグメント切替を描き `build(raw, labels, mode)` を呼ぶ (パネル4 が量/個人ペース補正で使用)。分析の追加は `analysis-panels.ts` へのパネル登録のみ (タブ・既存チャート部品は無改修。新しい描画形式が要るときのみチャート部品を 1 つ追加。パネル5 で `HeatmapTable` を追加した実例)。

### 入力項目
なし (表示専用。データは WBS の予定完了日 / 実績完了日 / 完了ステータスから自動集計)。

## 11.6 マイタスク画面

> **★実装ミラー (2026-06-03 改訂)★** 本節は `my-tasks-client.tsx` / `task.service.ts#listMyTaskProjects` の実装に同期。
> マイタスクは **WBS / 進捗確認画面 (§11.4 / §11.5) と同じツリー UI を流用** した個人ビュー。
> 旧仕様に記載されていたインライン編集 (進捗率/作業メモ/完了報告ボタン等) は実装に存在せず、**参照専用** (編集は各プロジェクトの WBS へ遷移して行う)。

### 画面目的
ログイン中ユーザが、**自分が担当するタスクを参加全プロジェクト横断で一覧確認**するための個人ビュー。WBS / ガントと同じ階層ツリー・折りたたみ・バッジ・列幅調整・状況フィルタ・横断ガント表示を流用する。

### マイタスク固有の特徴 (WBS / ガントとの違い)
| 観点 | WBS / ガント (§11.4 / §11.5) | マイタスク (§11.6) |
|---|---|---|
| 起点 | プロジェクト 1 件 | **ユーザ本人** |
| 表示範囲 | そのプロジェクトの全タスク | **自分担当の ACT + その祖先 WP のみ / 参加全プロジェクト横断** |
| グルーピング | なし (単一プロジェクト) | プロジェクト別セクション (折りたたみ可・プロジェクト名昇順) |
| 編集 | タスク CRUD・実績更新が可能 | 参照専用 (編集は各 WBS へ遷移) |

### データ取得仕様 (`listMyTaskProjects`)
- 対象抽出: `assigneeId = 本人` かつ `type = activity` (ACT) かつ未削除のタスクを持つプロジェクトを抽出 (WP は担当者を持たないため抽出キーにならない)。
- 各プロジェクトの全タスクを取得して階層ツリーを構築 → `filterTreeByAssignee` で「自分担当 ACT + その祖先 WP」だけに刈り込み。
- 完了タスクも取得対象 (状況フィルタの初期値が全状況選択のため既定で表示。フィルタで除外可)。
- テナント越境防止: `project.tenantId = 閲覧者テナント` を必須フィルタとして併記 (severity-1 多層防御)。

### 一覧項目 (列構成は WBS と統一)
| 項目 | 内容 |
|---|---|
| 名称 | WP/ACT バッジ + タスク名 + WBS番号。WP は折りたたみトグル。遅延 ACT は「遅延」バッジを付与 |
| ステータス | 未着手 / 進行中 / 完了 / 保留 (色分けバッジ) |
| 進捗&工数 | 進捗バー + 進捗率%。ACT は予定工数 (h) も併記 |
| 予定期間 | 開始予定日 〜 終了予定日 |
| 実績期間 | 実績開始日 〜 実績終了日 |

> **優先度列について (2026-06-03)**: 旧仕様にあった「優先度」列は **WBS と同様に撤去** (WBS は PR #63 で撤去済、本画面はその横展開)。優先度フィールド自体はデータとして残存し、内部の既定並び順 (期限昇順 → 優先度降順) にのみ使用。

### 入力項目
なし (参照専用。タスクの編集・実績更新は各プロジェクトの WBS 画面 §11.4 で行う)。

### 画面操作
| 操作 | 内容 | 実行可能ロール |
|---|---|---|
| プロジェクトセクション開閉 | クリックで展開/折りたたみ (sessionStorage 永続) | 全ロール (本人) |
| WP 折りたたみ | WBS と同じく WP は既定で折りたたみ | 全ロール (本人) |
| 状況フィルタ | 未着手/進行中/完了/保留 の複数選択 (既定=全選択) | 全ロール (本人) |
| 列ソート | 名称/ステータス/進捗/予定期間/実績期間 をクリックソート (top-level のみ) | 全ロール (本人) |
| 列幅調整・列幅リセット | ドラッグで列幅変更、「列幅をリセット」で初期化 | 全ロール (本人) |
| ガントチャート表示 | 「ガントチャートを表示」で横断ガント (プロジェクト毎に縦積み、担当者=自分で初期フィルタ) | 全ロール (本人) |
| ページネーション | プロジェクトグループ単位で 100 件/ページ | 全ロール (本人) |
| プロジェクトへ遷移 | プロジェクト名/タスクから親プロジェクトの WBS へ (編集はそちらで) | 全ロール (本人) |

### 状態・エラー
- 担当タスクが 1 件もない: 「担当しているタスクはありません」を表示。
- フィルタ該当なし: 「条件に一致するタスクがありません」を表示。
- サーバ取得失敗: 警告バナーを表示しつつ他画面への遷移は維持 (空表示で操作継続。エラーは system_error_logs に記録)。

### 権限
- **本人の担当タスクのみ閲覧可** (他人のマイタスクは参照不可。認可はセッションユーザ ID)。
- 全ロール (システム管理者 / PM/TL / メンバー / 閲覧者) が自分のマイタスクを利用可。画面自体に role gate はなく、表示内容は「自分が担当に設定されたタスクの有無」で決まる。

## 11.7 リスク / 課題一覧画面

### 画面目的
リスク・課題の可視化と追跡管理。

> **★2026-05-24 UI_PATTERNS §35 で統一★ 一覧 UI レイアウト**
> - プロジェクト配下 (CRUD) は **軽量テーブル + CrossListBulkVisibilityToolbar (visibility-only)** に統一。
>   旧 state+assigneeId+deadline 複合 bulk dialog (PR #161) は **撤廃** (state/assignee/deadline 一括変更は per-row 編集 dialog で行う)。
> - 5 一覧画面 (リスク/課題/ナレッジ/振り返り/メモ) の一括編集は全画面 visibility のみ。UI/API ともに統一。
> - 詳細: [docs/design/UI_PATTERNS.md §35](../design/UI_PATTERNS.md)

> **★2026-05-20 PR #416 で変更★ 削除権限の見直し**
> - 一覧タブ (プロジェクト詳細内) からの削除: **作成者本人のみ** (PM/TL も admin も他人作成は不可)
> - 全リスク / 全課題画面 (横断) からの admin 削除ボタンは **横断 DELETE 専用ルート** (`/api/risks/[id]` / `/api/issues/[id]`) を叩く (旧: project スコープルート経由 → 横断ルートへ統一)
> - 横断 DELETE 専用ルートは admin 特権でモデレーション削除を担当 (他テナント越境はガード継続)

### 一覧項目
| 項目 | 内容 |
|---|---|
| 種別 | リスク / 課題 |
| 件名 | タイトル |
| 内容要約 | 先頭表示 |
| 起票日 | 日付 |
| 起票者 | ユーザ名 |
| 関連タスク | タスク名 |
| 影響度 | 低/中/高 |
| 発生可能性 | 低/中/高 |
| 優先度 | 低/中/高 |
| 対応担当者 | ユーザ名 |
| 期限 | 日付 |
| 状態 | 未対応/対応中/監視中/解消 |
| 更新日時 | 最終更新 |

### 入力項目
| 項目 | 必須 | 備考 |
|---|---|---|
| 種別 | ○ | リスク / 課題 |
| 件名 | ○ | 100文字以内 |
| 内容 | ○ | 2000文字以内 |
| 原因 | × | 課題時に推奨 |
| 関連タスク | × | 1件以上可 |
| 影響度 | ○ | 低/中/高 |
| 発生可能性 | △ | リスク時必須 |
| 優先度 | ○ | 低/中/高 |
| 対応方針 | × | 1000文字以内 |
| 対応策 | × | 2000文字以内 |
| 対応担当者 | × | ユーザ選択 |
| 期限 | × | 日付 |
| 状態 | ○ | 選択式 |
| 結果 | × | 2000文字以内 |
| 教訓 | × | 2000文字以内 |

### ボタン制御
| ボタン | 企画中 | 見積中 | 計画中 | 実行中 | 実行可能ロール |
|---|---:|---:|---:|---:|---|
| 新規起票 | ○ | ○ | ○ | ○ | システム管理者、PM/TL、メンバー |
| 編集 | ○ | ○ | ○ | ○ | システム管理者、PM/TL、担当メンバー/起票メンバーは一部 |
| 保存 | ○ | ○ | ○ | ○ | 同上 |
| 状態変更 | × | △ | △ | ○ | システム管理者、PM/TL、担当メンバーは一部 |
| クローズ | × | △ | △ | ○ | システム管理者、PM/TL |
| 一覧CSV出力 | ○ | ○ | ○ | ○ | システム管理者、PM/TL |

## 11.8 ナレッジ一覧 / 詳細 / 登録画面

### 画面目的
知見の検索、参照、登録、公開。

> **★2026-05-24 UI_PATTERNS §35 で統一★ 一覧 UI レイアウト**
> - プロジェクト配下 (CRUD) は **軽量テーブル化** (旧カード形式から移行)。詳細 (背景/内容/結果等) は行クリックで KnowledgeEditDialog (readOnly 自動判定) を開いて表示。
> - 一括編集は **CrossListBulkVisibilityToolbar (visibility-only)**。5 一覧画面で同一 UI。
> - 詳細: [docs/design/UI_PATTERNS.md §35](../design/UI_PATTERNS.md)

> **★2026-05-20 PR #416 で変更★ 削除権限の見直し**
> - 一覧タブ (プロジェクト詳細内) からの削除: **作成者本人のみ** (PM/TL も admin も他人作成は不可)
> - 全ナレッジ画面 (横断) からの admin 削除ボタンは **横断 DELETE 専用ルート** (`/api/knowledge/[id]`) を叩く (旧: project スコープルート経由 → 横断ルートへ統一)
> - 横断 DELETE 専用ルートは admin 特権でモデレーション削除を担当 (他テナント越境はガード継続)

### 一覧項目
| 項目 | 内容 |
|---|---|
| タイトル | ナレッジ名 |
| 種別 | 調査/検証/障害対応/教訓等 |
| 対象プロジェクト | 案件名 |
| 対象工程 | 要件定義/設計等 |
| 対象技術 | 技術タグ |
| 開発方式 | スクラッチ等 |
| 公開状態 | 下書き/公開 |
| 作成者 | ユーザ名 |
| 更新日時 | 最終更新 |
| 再利用性 | 低/中/高 |

### 入力項目
| 項目 | 必須 | 備考 |
|---|---|---|
| タイトル | ○ | 150文字以内 |
| ナレッジ種別 | ○ | 選択式 |
| 対象プロジェクト | △ | 任意だが推奨 |
| 対象工程 | × | 複数可 |
| 対象技術 | × | タグ形式 |
| 開発方式 | × | 選択式 |
| 背景 | ○ | 2000文字以内 |
| 内容 | ○ | 5000文字以内 |
| 結果 | ○ | 3000文字以内 |
| 結論 | × | 2000文字以内 |
| 推奨事項 | × | 2000文字以内 |
| 再利用性 | × | 低/中/高 |
| 関連タスク | × | 複数可 |
| 関連リスク | × | 複数可 |
| 関連見積項目 | × | 複数可 |
| 関連リンク | × | URL複数可 |
| 公開範囲 | ○ | 下書き/プロジェクト限定/社内公開 |

### ボタン制御
| ボタン | 実行可能状態 | 実行可能ロール |
|---|---|---|
| 新規作成 | 企画中〜実行中 | システム管理者、PM/TL、メンバー |
| 下書き保存 | 企画中〜実行中 | システム管理者、PM/TL、メンバー |
| 編集 | 企画中〜実行中 | システム管理者、PM/TL、作成メンバー |
| 公開 | 見積中〜実行中 | システム管理者、PM/TL |
| 公開範囲変更 | 見積中〜実行中 | システム管理者、PM/TL |
| 論理削除 | 企画中〜実行中 | システム管理者、PM/TL |
| 検索 | 全状態 | 全ロール |
| 詳細表示 | 全状態 | 公開範囲に応じる |

## 11.9 振り返り画面

### 画面目的
完了後の総括と、次案件へつなぐ知見整理。

> **★2026-05-24 UI_PATTERNS §35 で統一★ 一覧 UI レイアウト**
> - プロジェクト配下 (CRUD) は **軽量テーブル化** (旧カード形式から移行)。詳細 (計画/実績総括, goodPoints, problems, improvements) は行クリックで RetrospectiveEditDialog (readOnly 自動判定) を開いて表示。
> - 一括編集は **CrossListBulkVisibilityToolbar (visibility-only)**。5 一覧画面で同一 UI。
> - 詳細: [docs/design/UI_PATTERNS.md §35](../design/UI_PATTERNS.md)

> **★2026-05-20 PR #416 で変更★ 削除権限 + linkedProjects per-link gate**
> - 一覧タブ (プロジェクト詳細内) からの削除: **作成者本人のみ** (PM/TL も admin も他人作成は不可)
> - 全振り返り画面 (横断) からの admin 削除ボタンは **横断 DELETE 専用ルート** (`/api/retrospectives/[id]`) を叩く (旧: project スコープルート経由 → 横断ルートへ統一)
> - 横断 DELETE 専用ルートは admin 特権でモデレーション削除を担当 (他テナント越境はガード継続)
> - **linkedProjects[].name の per-link gate**: 振り返りに紐付く他プロジェクト名を、閲覧者が当該プロジェクトの ProjectMember でない場合は `null` でマスクして API レスポンスを返す。UI 側は null の時「非公開のプロジェクト」と表示してリンクも無効化 (テナント越境情報漏洩防止)

### 一覧項目
| 項目 | 内容 |
|---|---|
| 対象プロジェクト | 案件名 |
| 実施日 | 日付 |
| 実施者 | 作成者 |
| 振り返り状態 | 未作成/作成中/確定 |
| 見積差分要因数 | 件数 |
| 改善事項数 | 件数 |
| 更新日時 | 最終更新 |

### 入力項目
| 項目 | 必須 | 備考 |
|---|---|---|
| 実施日 | ○ | 日付 |
| 計画総括 | ○ | 2000文字以内 |
| 実績総括 | ○ | 2000文字以内 |
| 良かった点 | ○ | 3000文字以内 |
| 問題点 | ○ | 3000文字以内 |
| 見積差分要因 | × | 3000文字以内 |
| スケジュール差分要因 | × | 3000文字以内 |
| 品質面課題 | × | 3000文字以内 |
| リスク対応評価 | × | 3000文字以内 |
| 次回改善事項 | ○ | 3000文字以内 |
| 横展開したい知見 | × | 3000文字以内 |
| 振り返りコメント | × | コメント形式 |

### ボタン制御
| ボタン | 実行可能状態 | 実行可能ロール |
|---|---|---|
| 新規作成 | 実行中○ | システム管理者、PM/TL |
| 編集 | 実行中○ | システム管理者、PM/TL |
| コメント投稿 | 実行中○ | システム管理者、PM/TL、メンバー |
| 確定 | 実行中○ | システム管理者、PM/TL |
| 詳細表示 | 実行中以降 | 全ロール |

## 11.10 プロジェクトメンバー管理画面

### 画面目的
プロジェクトへの参加ユーザとプロジェクトロールを管理。

> **★2026-05-20 PR #416 で変更★ PM/TL に自律権限を付与**
> - **PM/TL もメンバー追加・削除・ロール変更が可能** (旧: admin のみ → PM/TL の自律運営を可能にする)
> - ただし **PM/TL ロールそのものを扱う操作 (PM/TL を追加 / 既存 PM/TL のロール変更 / PM/TL を削除) は admin のみ** (PM/TL が自分の上長権限を増減できないようにするガード)
> - メンバー追加候補のユーザ一覧は `GET /api/projects/[id]/available-users` から取得 (テナント内かつ未参加のユーザのみ。PM/TL も呼べる API)
> - 下記「ボタン制御」表の「実行可能ロール」欄は本変更で更新済

### 一覧項目
| 項目 | 内容 |
|---|---|
| ユーザ名 | 氏名 |
| メールアドレス | ログインID |
| プロジェクトロール | PM/TL / メンバー / 閲覧者 |
| 利用状態 | 有効/無効 |
| 設定者 | 変更者 |
| 更新日時 | 最終更新 |

### 入力項目
| 項目 | 必須 | 備考 |
|---|---|---|
| ユーザ | ○ | 既存ユーザ選択 |
| プロジェクトロール | ○ | 選択式 |
| 備考 | × | 1000文字以内 |

### ボタン制御 (PR #416 で更新)
| ボタン | 実行可能ロール | 状態制約 |
|---|---|---|
| メンバー追加 (member / viewer ロール) | システム管理者、PM/TL | クローズ以外 |
| メンバー追加 (PM/TL ロール) | システム管理者のみ | クローズ以外 |
| ロール変更 (member ↔ viewer 間) | システム管理者、PM/TL | クローズ以外 |
| ロール変更 (PM/TL を含む) | システム管理者のみ | クローズ以外 |
| メンバー解除 (member / viewer 対象) | システム管理者、PM/TL | クローズ以外 |
| メンバー解除 (PM/TL 対象) | システム管理者のみ | クローズ以外 |
| 一覧表示 | システム管理者、PM/TL | 全状態 |
| 保存 | システム管理者、PM/TL (上記操作可能範囲のみ) | クローズ以外 |

## 11.11 ユーザ管理画面

> **詳細設計の真値**: [design/USER_MANAGEMENT.md](../design/USER_MANAGEMENT.md)（ライフサイクル・席数・API・編集ダイアログ・監査を網羅）。本節は画面仕様の要約。

### 画面目的
テナント内ユーザの **発行・編集・無効化・削除・ロック解除・招待管理**。

> **★2026-06-03 更新★ アカウント状態 3 区分 + 招待管理 + 監査列**
> - アカウント状態を **招待中 / 有効 / 無効** の 3 状態で表示（`deriveAccountStatus`：招待中 = 招待受諾日時 `invitationAcceptedAt` が NULL、有効/無効 = 受諾済 + `isActive`）。**ロック（連続失敗の自動保護）は状態とは別軸**で「認証ロック」列に表示。
> - 招待中ユーザも一覧に表示（旧設計は `deletedAt` で代用し非表示だった）。編集ダイアログの「招待中」セクションから **招待メール再送 / 招待取消**（席を解放）。
> - 前回ログイン・作成者・作成日時・更新者・更新日時の列を表示。作成日時/更新日時は監査列のため `formatDateTimeSeconds`（`YYYY/MM/DD HH:mm:ss`）。
> - 編集ダイアログに **前回ログイン / MFA 有無 / リカバリーコード再発行**（MFA 設定済みのみ・10 個を 1 回表示）。
> - **画面見出し（「ユーザ管理」）は非表示**。
> - 席数（Beginner、案A）は **有効 + 招待中** を予約として数える（課金用 `activeUserCount` とは別概念）。

> **★アクセス権 (feat/crud-permission-redesign 2026-05-20)★**: 本画面・監査ログ・権限変更履歴の到達判定は `isAdminOrAbove` (= admin **+ super_admin**) に統一済。旧実装の `=== 'admin'` 厳密比較で super_admin が UI に到達できなかった UI/API ズレを解消 (API 側 `requireAdmin` と整合)。以下「実行可能ロール」欄の「システム管理者」は super_admin を含む。

> **★2026-05-20 PR #416 で変更★ 自己ロール変更禁止 + super_admin オプション非表示**
> - 編集ダイアログで **自分自身のロール変更フィールドは disabled** (admin が自分の admin 権限を剥奪する事故防止)
> - システムロール選択肢から **`super_admin` を非表示** (super_admin は seed / 運用手順でのみ付与する内部ロールで、UI からは付与不可)
> - サーバ側でも対応ガード (自己ロール変更 / super_admin 指定は 400 / 403 で拒否)

### 一覧項目（左から順）
| 項目 | 内容 |
|---|---|
| ユーザ名 | 氏名 |
| メールアドレス | ログインID |
| ロール | 管理者/一般ユーザ（バッジ） |
| ステータス | アカウント状態（招待中 / 有効 / 無効） |
| 認証ロック | 永続ロック / 一時ロック (PW・MFA、解除予定 tooltip) / 失敗カウント / —（状態とは別軸） |
| 前回ログイン | 最終ログイン成功日時（未ログインは「未ログイン」） |
| 作成者 | 招待した管理者の氏名（記録なしは「—」） |
| 作成日時 | 登録日時（`YYYY/MM/DD HH:mm:ss`） |
| 更新者 | 最後に編集した管理者の氏名（記録なしは「—」） |
| 更新日時 | 最終更新日時（`YYYY/MM/DD HH:mm:ss`） |

- 検索: 氏名・メールの部分一致（`?keyword=` で URL 永続化）。並び替え: 各列。

### 入力項目
| 項目 | 必須 | 備考 |
|---|---|---|
| ユーザ名 | ○ | 100文字以内 |
| メールアドレス | ○ | 重複不可 |
| システムロール | ○ | 選択式 |
| 利用状態 | ○ | 有効/無効 |

### ロック情報 (PR #85 追加)
ユーザ編集ダイアログ内に「ログインロック情報」セクションを表示:
- ログイン失敗回数 (閾値: `LOGIN_FAILURE_MAX`=5)
- 一時ロック解除予定日時 (失敗 5 回で `TEMPORARY_LOCK_DURATION_MS`=30 分の自動ロック)
- 永続ロックの有無
- ロック解除ボタン (`POST /api/admin/users/:id/unlock` 呼び出し。`failedLoginCount` / `lockedUntil` / `permanentLock` を全クリア)

### ボタン制御（すべてシステム管理者 = admin + super_admin のみ）
| ボタン | API | 備考 |
|---|---|---|
| 新規ユーザ登録 | `POST /api/admin/users` | 席数満席時は disabled |
| 保存（氏名/ロール/状態） | `PATCH /api/admin/users/:id` | 自己ロール変更・super_admin 昇格は拒否 |
| ロック解除 | `POST /api/admin/users/:id/unlock` | PW + MFA ロックを一括クリア。非招待中のみ |
| リカバリーコードを再発行 | `POST /api/admin/users/:id/recovery-codes` | MFA 設定済み・非招待中。10 個を 1 回表示 |
| 招待メールを再送 | `POST /api/admin/users/:id/resend-invitation` | 招待中のみ |
| 招待を取り消す | `POST /api/admin/users/:id/cancel-invitation` | 招待中のみ。物理削除し席を解放 |
| このユーザを削除 | `DELETE /api/admin/users/:id` | 論理削除 + メンバー等カスケード。非招待中・自己不可 |
| 非アクティブユーザをロック | `POST /api/admin/users/lock-inactive` | 30 日無アクティブの非 admin を一括無効化（自テナント） |

## 11.11b 顧客管理画面 (PR #111-2 完全導入)

### 画面目的
プロジェクトの発注元である顧客 (Customer) のマスタ管理。PR #111-1 で新設し、
PR #111-2 で `Project.customer_name` を廃止して `customer_id` FK に完全移行した。

### 一覧項目 (`/customers`、admin のみ)
| 項目 | 内容 |
|---|---|
| 顧客名 (リンク) | クリックで詳細画面へ |
| 部門 | 部署・部門名 |
| 担当者 | 連絡窓口の氏名 |
| メール | 担当者メール |
| 紐付プロジェクト | active Project 件数 (Badge) |
| 削除 | active Project なら詳細画面のカスケード削除へ誘導 / 0 件なら confirm のみで物理削除 |

### 詳細画面 (`/customers/[customerId]`)
- 顧客情報 (name / department / contactPerson / contactEmail / notes)
- 紐付 active プロジェクト一覧 (projects テーブル経由 1:N)
- 編集: インラインダイアログで PATCH /api/customers/[id]
- 削除 (カスケード対応):
  - active Project 0 件 → 単純物理削除
  - active Project 1+ 件 → カスケードダイアログ表示
    - 常時削除: Project 本体 / WBS / 見積 / メンバー / 添付
    - オプション: リスク / 課題 / 振り返り / ナレッジ (他プロジェクト共有のナレッジは unlink)

### 入力項目
| 項目 | 必須 | 備考 |
|---|---|---|
| 顧客名 | ○ | 100文字以内、ユニーク制約なし (同名併存可) |
| 部門 | × | 100文字以内 |
| 担当者 | × | 100文字以内 |
| 担当者メール | × | 255文字以内、メール形式 |
| 備考 | × | 1000文字以内 |

### ボタン制御
| ボタン | 実行可能ロール |
|---|---|
| 一覧表示 | システム管理者 |
| 新規登録 | システム管理者 |
| 編集 | システム管理者 |
| 削除 (単純) | システム管理者 (active Project 0 件) |
| 削除 (カスケード) | システム管理者 (active Project 1+ 件時に cascadeRisks/Issues/Retros/Knowledge 選択) |

### 削除の FK 挙動
- `Project.customer_id` は `ON DELETE SET NULL`
- 論理削除済 Project (`deletedAt IS NOT NULL`) は Customer 物理削除時に customer_id のみ null 化 (監査・振り返り参照のため本体は残す)
- active Project はカスケードで先に物理削除してから Customer を削除する (deleteCustomerCascade)

---

## 11.12 権限変更履歴画面

> 詳細設計 (記録スコープ・表示派生) は [design/OBSERVABILITY.md §1.2](../design/OBSERVABILITY.md)、テーブル定義は [design/DATA_MODEL.md §8.26](../design/DATA_MODEL.md)。

### 画面目的
誰が誰の権限・状態をいつどう変更したかの監査。**画面見出しは非表示** (2026-06-03、監査ログと統一)。

### 一覧項目
| 項目 | 内容 |
|---|---|
| 日時 | 変更日時 |
| 変更者 | 管理者名 |
| 対象ユーザ | 被変更者 |
| 種別 | システムロール / プロジェクトロール (ロケール表示) |
| 変更前 | 前ロール (テナント管理者 / 一般ユーザ / PM/TL / メンバー 等) または状態 (有効/無効)。初回付与は「-」 |
| 変更後 | 後ロール または 有効 / 無効 / 削除 / 解除 (すべてロケール表示) |
| 理由 | 「ユーザ新規登録」「システムロール変更」「アカウント有効化/無効化」「ユーザ削除」等 |

### 記録スコープ
- **システムロール**: ユーザ新規登録 (初期ロール) / ロール変更 / **有効化・無効化** / ユーザ削除 / 新規テナント作成時の初期 admin。
- **プロジェクトロール**: メンバー追加 / ロール変更 / 解除 (ユーザ削除に伴う除去含む)。
- CSV エクスポートは **未実装** (旧記載は誤り、2026-06-03 是正)。

### 操作・絞り込み・件数 (2026-06-03、監査ログ §11.13 と統一)
| 機能 | 内容 |
|---|---|
| 絞り込み | 変更者 / 対象ユーザ / 種別 のプルダウン + キーワード検索 (変更者・対象・種別・前後ロール・理由を横断) |
| 並び替え | 各列の見出しクリック (複数列・sessionStorage 永続化) |
| 列幅調整 | 各列の境界をドラッグで幅変更 + 「列幅をリセット」(表の右上・統一位置、`ResizableTableShell`) |
| 表示件数 | 既定 最新 300 件。`100 / 300 / 1000 / 全件` を選択 (`?limit=`) |
| ページネーション | 100 行/ページ (表の直下・中央)。絞り込み変更で先頭ページに戻る |

### 入力項目
| 項目 | 必須 | 備考 |
|---|---|---|
| 変更理由 | × | 1000文字以内 |

### ボタン制御
| ボタン | 実行可能ロール |
|---|---|
| 一覧表示 / 絞り込み / 並び替え / 列幅調整 / 表示件数変更 / ページ送り | システム管理者 |

---

## 11.13 監査ログ画面

> 詳細設計 (記録レイヤ・スコープ・表示派生) は [design/OBSERVABILITY.md §1.1](../design/OBSERVABILITY.md)。本節は画面仕様の要約。

### 画面目的
テナント内の **業務データ操作 (作成 / 更新 / 削除)** を新しい順に確認する。アクセス権 = システム管理者 (admin + super_admin)。

### 一覧項目
| 項目 | 内容 |
|---|---|
| 日時 | 操作日時 |
| 操作者 | 操作した管理者/メンバーの氏名 |
| 操作 | 作成 / 更新 / 削除 / 同期取込 / 一括更新 等 (ロケール表示)。添付は「リンク追加 / リンク削除 / ファイル添付 / ファイル削除」 |
| 対象 | 操作対象の種別 (プロジェクト / リスク / 課題 / ナレッジ / 振り返り / ステークホルダー / 見積 / 顧客 / コメント / メモ / メンバー / ユーザ 等、ロケール表示)。**添付・リンクは親画面の種別 (どこで行われたか)** を表示 |
| 対象名 | 対象の名前 (プロジェクト名・タイトル等)。名前を持たないもの (振り返り・見積・添付等) は「—」 |

- 「リスク」「課題」は別行として区別表示 (記録の type で振り分け)。**対象ID列は表示しない** (人が読めないため)。

### 操作・絞り込み・件数
| 機能 | 内容 |
|---|---|
| 絞り込み | 操作 / 対象 / 操作者 のプルダウン + キーワード検索 (対象名・操作者・操作・対象を横断) |
| 並び替え | 各列の見出しクリック (複数列・sessionStorage 永続化) |
| 列幅調整 | 各列の境界をドラッグで幅変更 + 「列幅をリセット」ボタン (表の右上・全画面統一位置、`ResizableTableShell`) |
| 表示件数 | 既定 最新 300 件。`100 / 300 / 1000 / 全件` を選択 (`?limit=` でサーバ再取得) |
| ページネーション | 100 行/ページ (表の直下・中央)。絞り込み変更で先頭ページに戻る |

### スコープ (記録対象 / 対象外)
- **記録対象**: 業務データ (上記エンティティ) の作成・更新・削除、リンク/ファイルの添付・削除、CSV 一括取込・一括公開範囲変更。
- **対象外 (この画面には出ない)**: ① 閲覧・検索・エクスポート (read 系は非記録) ② ログイン・MFA・パスワード等の認証イベント (別の `auth_event_logs`、最終ログインはユーザ管理の「前回ログイン」) ③ システムロール変更の詳細 (専用「権限変更履歴」§11.12) ④ テナント設定・課金・解約の変更 (現状未記録、将来対応候補)。

### ボタン制御
| ボタン | 実行可能ロール |
|---|---|
| 一覧表示 / 絞り込み / 並び替え / 表示件数変更 / ページ送り | システム管理者 |

---

## 11.14 テナント設定画面

> 詳細設計 (全セクション・データ入出力の区別・解約・課金参照) は [design/TENANT_SETTINGS.md](../design/TENANT_SETTINGS.md)、公開向けは [public/tenant-settings-guide.md](../public/tenant-settings-guide.md)。

### 画面目的
テナント全体のプラン・使用量・請求・データ入出力・解約の管理。**アクセス = テナント管理者 (admin) のみ**（super_admin / 一般ユーザはリダイレクト）。DB 容量 / API 利用量は画面を開いた時点で再集計（右上に再集計ボタン）。

### タブ・セクション構成
| タブ | セクション |
|---|---|
| 概要 | プラン (Beginner/Expert/Pro、Beginner 降格不可・Expert↔Pro 即時) / 言語・タイムゾーン (テナント全体) / スターターデータ (見本データ) の取込・一括削除 (Expert/Pro は容量従量課金の確認→承認) / データエクスポート (ZIP) / データインポート (自サービス ZIP の復元・移行) / 詳細情報 (UUID・作成日時・単価) / テナント解約 (危険操作)。※ 旧「提案エンジン: シードデータ参照」トグルは 2026-06-05 撤去 (単一テナント化) |
| 使用量 | embedding 未生成バナー / 生成 AI 系利用量 (当月 LLM・Embedding) / 月次予算上限 (Expert/Pro のみ) / DB 系利用量 (DB 容量・ファイルストレージ、現在/月中 peak/想定請求) |
| 請求 | 今月請求金額 (LLM/Embedding/DB 超過/Storage 超過/合計) / 請求先情報 (法人・個人/住所/支払い方法) / 支払い方法 (銀行振込・クレカ) / 請求履歴 (子画面 `/settings/tenant/billing`、直近 6 ヶ月) |

### データ入出力の区別
- **データエクスポート**: 全業務データの ZIP バックアップ。
- **データインポート**: **自サービス出力 ZIP の復元/テナント間移行専用**。
- **CSV データ移行** (`/settings/tenant/migration-import`): Excel/旧 PM ツール等の **初回移行** (7 種 CSV、4 ステップ)。
- **API 連携インポート** (`/settings/tenant/api-import`、ベータ): Notion/Backlog/kintone/Pleasanter/Google スプレッドシートに直接接続して取込。

### 監査
- プラン変更・予算上限・請求先・解約等の操作は **現状 `audit_logs` (監査ログ画面) に記録されない** (将来対応候補)。課金表示は `ApiCallLog` SUM の真値で reconcile。

---

## 提案型サービス v1 画面仕様 (PR #65)

## 16. 核心機能: 提案型サービス (PR #65)

### 16.1 機能概要

本プロダクトの核心価値である「**過去の資産を未来のプロジェクトに自動で活かす**」機能。
人手で過去資産を探す限り発生する抜け漏れを極小化し、未然に防げた課題の顕在化を減らす。
技術詳細は DESIGN.md §23 を参照。

### 16.2 対象資産

| 資産 | 絞り込み | 理由 |
|---|---|---|
| ナレッジ (Knowledge) | `visibility='public'` + **自プロジェクト未紐付け** (PR #160) | 全社共有済の知見のみ候補化。自プロジェクトに紐付け済のナレッジは「参考」として並べる意味がない (Issue/Retrospective と parity) |
| 過去の課題 (Issue) | `type='issue'` + `state='resolved'` + 他プロジェクト | リスクは未発生のため除外、解消済課題のみ過去資産として扱う |

### 16.3 画面・操作

| 画面 / タイミング | 動作 |
|---|---|
| 新規プロジェクト作成直後 | `/projects/[id]?suggestions=1` に遷移 → 提案モーダルが自動起動 |
| プロジェクト詳細「参考」タブ | 常時参照可能、タブ切替で即座に提案リスト表示 |

### 16.4 採用操作

| 対象 | ボタン | 挙動 |
|---|---|---|
| Knowledge | 「このプロジェクトに紐付け」 | `knowledge_projects` に中間レコード追加。採用直後はその場で「紐付け済」バッジに切替、再フェッチ時は提案候補から除外される (PR #160) |
| 過去 Issue | 「雛形として採用」 | 新規 Issue を type='issue' / state='未対応' / visibility='下書き' で複製。起票者は採用者に差し替え、`result`・`lessonLearned` は継承しない |

### 16.5 権限 (★2026-05-20 PR #416 で変更★)

| ロール | 「参考」タブ表示 | 閲覧 (GET) | 採用 (POST adopt) |
|---|---|---|---|
| システム管理者 | ○ | 全プロジェクト | 全プロジェクト |
| プロジェクト PM/TL | ○ | 自プロジェクト | 自プロジェクト |
| プロジェクト member | **×** (タブ非表示) | **不可 (API 403)** | **不可 (API 403)** |
| プロジェクト viewer | **×** (タブ非表示) | **不可 (API 403)** | **不可 (API 403)** |
| 非メンバー | × | 不可 | 不可 |

**変更理由**: 提案エンジン経由の他プロジェクト資産参照は、プロジェクトの意思決定 (採用判断) を伴うため PM/TL 以上の権限者に限定する。member / viewer は「参考」タブ自体を非表示にし、API も 403 を返して UI 迂回を防ぐ (curl 直叩き不可)。
adopt 操作 (採用ボタン) も同じく PM/TL + admin のみ。

### 16.6 精度指標 (内部表示)

提案 1 件ごとに以下の 3 つのスコアを保持し、tooltip で根拠を表示する。
- `tagScore`: タグ Jaccard 係数 (0〜1)
- `textScore`: pg_trgm テキスト類似度 (0〜1)
- `score`: 上記の重み付き平均 (既定 50%/50%)

### 16.7 プロジェクトタグの入力仕様

新規プロジェクト作成時に以下の 3 種類のタグをカンマ区切りで入力できる。
提案精度向上のため入力を推奨する。

| タグ種別 | 例 | マッチング対象 |
|---|---|---|
| 業務ドメイン | 金融 / 基幹業務 / 会計 | Knowledge.businessDomainTags (Phase 2 で対応完了) |
| 技術スタック | React / Next.js / TypeScript / PostgreSQL | Knowledge.techTags |
| 工程 | 要件定義 / 設計 / 開発 / 試験 | Knowledge.processTags |

### 16.8 Phase 2 で追加された機能 (同 PR #65)

**過去振り返りの提示**: プロジェクト詳細「参考」タブ + 新規作成後モーダルに、類似する過去振り返りの問題点・次回事項を参考情報として表示 (採用操作なし)。

**ナレッジ作成時のタグ入力**: ナレッジ作成フォームに 3 種類のタグ入力欄 (業務ドメイン / 技術スタック / 工程) を追加。将来のプロジェクトからの類似検索精度を上げる。

**リスク起票時の inline 提案**: リスク/課題 起票ダイアログで、件名・内容を入力中に 500ms debounce で類似する過去課題 (他プロジェクト) を検索し、警告バナーとして表示。「過去に同種の事象があるので確認を」と気付きを与える UX。

---


## 一覧列幅のドラッグリサイズ (PR #68)

## 17. 一覧列幅のドラッグリサイズ (PR #68)

### 17.1 目的

各一覧/全〇〇画面で長文列 (タスク名 / 件名 / 顧客名 等) が切り詰められ、ユーザが中身を確認できない問題を **UI 上のドラッグ操作**で解消する。

### 17.2 操作

- 列ヘッダの右端 (6px 幅) にカーソルを近づけると `col-resize` カーソルに変化
- マウスダウン + ドラッグで任意の幅に変更
- 各一覧の右上にある「列幅をリセット」ボタンで既定値に戻す

### 17.3 対象画面

| 画面 | 対応 |
|---|---|
| `/projects` | 全列リサイズ可 |
| `/risks` / `/issues` | 全列リサイズ可 |
| `/retrospectives` | 全列リサイズ可 |
| `/knowledge` | 全列リサイズ可 |
| プロジェクト詳細: リスク/課題/WBS 一覧 | 全列リサイズ可 |
| `/my-tasks` | 全列リサイズ可 |
| プロジェクト詳細: ガント | **タスク名列のみ**リサイズ可 (日付列は固定) |

### 17.4 永続化ポリシー

- 幅は **sessionStorage** (`resizable-cols:<tableKey>` / `gantt:<projectId>:name-col-width`) に保存
- **タブ/ブラウザを閉じると既定値に戻る** (既存の折りたたみ状態・フィルタと同じ方針)
- 異なるブラウザ/端末では共有されない (ユーザ個人単位)

### 17.5 制約

- 最小 40px / 最大 800px でクランプ (テキスト完全潰れ回避 / 横スクロール暴走防止)

---


## WBS 「集計再計算」ボタンの UI 撤去 (PR #68)

## 18. WBS 「集計再計算」ボタンの UI 撤去 (PR #68)

### 18.1 変更内容

プロジェクト詳細の WBS 画面に配置されていた「集計再計算」ボタンを撤去した。
全 WP の集計値 (担当者 / 進捗 / 日程 / ステータス) はタスクの更新時に自動で再計算されるため、手動ボタンの UI 露出は運用上不要と判断。

### 18.2 温存した要素

- API ルート `POST /api/projects/:id/tasks/recalculate` は温存
- 管理者がトラブルシュート時に curl 等で直接叩ける手段として残す

---


## タスクの状況 / 進捗率 整合性ルール (PR #69 Task 1)

## 19. タスクの状況 / 進捗率 整合性ルール (PR #69 Task 1)

### 19.1 既存ルール (PR #39 から)

- **状況 = 完了 → 進捗率 = 100 を強制**
  - UI のステータス変更時に進捗率を 100 に自動補正
  - サーバ側 (updateTask / bulkUpdateTasks / updateTaskProgress) でも同様に正規化

### 19.2 追加ルール (PR #69)

- **進捗率 = 100 → 状況 = 完了 を強制** (逆方向も双方向に整合)
  - UI で進捗率を 100% に入力した時点で状況が自動で「完了」に切り替わる
  - サーバ側でも同ルールを検証/強制 (不正な "進捗 100% + 状況 = 進行中" 等の組合せを防止)

### 19.3 競合時の優先順位

同一リクエスト内で status と progressRate の両方が矛盾して指定された場合:
- `progressRate=100` と `status≠completed` の組 → **「進捗 100% ならば完了」を優先** (業務ルールとして矛盾する組合せを許容しない)
- `progressRate<100` と `status=completed` の組 → 既存通り「完了 → 進捗 100%」を優先

---


## ナビゲーション構成の調整 (PR #69 Task 3)

## 20. ナビゲーション構成の調整 (PR #69 Task 3)

### 20.1 変更前

画面上部のナビ: 「プロジェクト」「マイタスク」「全リスク」「全課題」「全振り返り」「全ナレッジ」

### 20.2 変更後

- **マイタスク**をナビから撤去し、画面右上のユーザ名プルダウンメニュー (AccountMenu) 内に移動
- プルダウン内の並び (現行 `app-header.tsx` の `AccountMenu`): **マイタスク** → メモ → 設定 → (区切り) → ガイド → ヘルプ → **バージョンアップ情報** (→ `/changelog`) → サービス紹介ページ → Discord → (区切り) → ログアウト
- 意図: マイタスクはユーザ個人専用画面のため、共有ナビではなくアカウント文脈に寄せる
- **バージョンアップ情報** (ADR-0031 / 2026-05-31): 旧フッタ + 旧 `/settings/about` にあった「バージョン / 更新履歴」導線をここに移設。ログイン後のみ到達できる AccountMenu 内に置き、未ログイン画面では露出させない。リンク先はアプリ内 `/changelog`

### 20.3 顧客管理の先頭配置 (PR #111-2 後追い)

admin ログイン時のナビ並びは左から順に:

1. **顧客管理** (admin のみ、PR #111-2 で最左に移動)
2. プロジェクト
3. 全リスク / 全課題 / 全振り返り / 全ナレッジ / 全メモ
4. ユーザ管理 / 監査ログ / 権限変更 (admin のみ、運用管理系)

- **意図**: 顧客管理は概念的にプロジェクトの「親」 (1:N 関係) のため、情報ヒエラルキ上
  プロジェクトより左に置く。ユーザ管理等の運用管理メニューは従来通り末尾にまとめる。
- **非 admin 表示**: 顧客管理 / ユーザ管理 / 監査ログ / 権限変更はすべて非表示。
  ナビは「プロジェクト」から開始する。
- **実装** (feat/app-header-footer-unification 2026-05-24 以降): `src/components/app-header.tsx`
  の `navGroupsConfig` で「プロジェクト」「資産」「システム管理者」「super_admin」の 4 グループに
  分類し、admin 用項目は `adminOnly` フラグで描画制御。旧 `dashboard-header.tsx` は AppHeader に
  統合済 (= ログイン前後の表示分岐 + auto-hide + nowrap 等を含む)。

### 20.4 3 分類グループ dropdown ナビ (PR #127 / v1.5.0 で常時 dropdown 化)

PR #127 でナビを **3 分類** にグループ化。v1.5.0 より**画面幅に関係なく常時グループ dropdown 表示**に統一（旧 xl: ブレークポイントによるフラット/dropdown ハイブリッド切替は廃止）。

#### 分類

| グループ | 配下項目 | 備考 |
|---|---|---|
| **プロジェクト** | 全プロジェクト / **全顧客管理 (admin のみ)** | 全見積もり / 全 WBS は未実装 (DEVELOPER_GUIDE §11 T-15/T-16 で追跡) |
| **資産** | 全リスク / 全課題 / 全振り返り / 全ナレッジ / 全メモ | プロジェクト横断の知見・気付き。**5 画面とも統一レイアウト規約に準拠** (UI_PATTERNS §34) |
| **システム管理者 (admin のみ)** | ユーザ管理 / 監査ログ / 権限変更 | 運用管理系 |

##### 「資産」5 画面の統一レイアウト (feat/all-list-section-unification, 2026-05-24)

全リスク / 全課題 / 全振り返り / 全ナレッジ / 全メモ の 5 画面は以下のセクション順に統一:

1. **件数行** (フィルタ後件数 / `common.itemCount` / 右寄せ)
2. **FilterBar** (検索 + 画面固有のフィルタ軸 / `<Label>` 付き)
3. **テーブル** (`ResizableTableShell` + `SortableResizableHead` + `ClickableRow`)
4. **詳細ダイアログ** (read-only。memo のみ専用 `MemoViewDialog`、他 4 画面は `XxxEditDialog readOnly={true}`)

検索入力の `data-testid` は `all-{entity}-search-input` (entity は複数形) に統一。
詳細は [docs/design/UI_PATTERNS.md §34](../design/UI_PATTERNS.md) 参照。

#### 表示方式 (v1.5.0)

画面幅によらず**常時グループ dropdown** (Base UI Menu) を使用する。グループトリガをクリックすると配下の nav 項目が dropdown で表示される。

#### アクティブ表示

- グループトリガ (「プロジェクト」「資産」「システム管理者」) は配下のいずれかが現在ページなら `bg-accent + font-medium` で強調
- dropdown 内の子項目も現在ページなら同様に強調

#### 認可 (多層防御)

- UI 層: `adminOnly: true` の項目 / グループは `session.user.systemRole === 'admin'` のみ表示
- API 層: 既存の認可ロジック (admin 専用 API ルート) で再判定
- 前者だけに依存しない (UI 非表示は利便性、認可はサーバで担保)

#### 実装

`src/components/app-header.tsx` (旧 `dashboard-header.tsx`、feat/app-header-footer-unification 2026-05-24 で統合):
- `navGroupsConfig: NavGroupConfig[]` に 3 分類 + super_admin 1 分類の階層構造
- `GroupMenu` コンポーネント: Base UI `Menu.Root/Trigger/Portal/Positioner/Popup/Item` で構築、画面幅によらず常時使用
- `groupedNavClass = 'flex'` で常時表示 (v1.5.0: 旧 `hidden xl:flex` / `xl:hidden` の切替は廃止)
- 全 nav グループトリガに `whitespace-nowrap` を付与しラベルの 1 行表示を担保

### 20.5 共通フッタ (AppFooter / 認証状態で出し分け — ADR-0031 / 2026-05-31)

全画面共通フッタ (`src/components/app-footer.tsx`、Server Component) は `isAuthenticated` prop で 2 層に出し分ける。認証状態は root layout が `auth()` で解決して渡す (MFA 未検証は `false` 扱いで共通情報のみ表示)。すべてのリンクは外部 LP (tasukiba-user ページ) の各アンカーへ集約し、サービス内に二重に持たない (LP を単一真値とする)。

| 層 | 表示条件 | リンク | 遷移先 |
|---|---|---|---|
| **共通情報** | ログイン前後で常時表示 | 製品ページ / 利用規約 / プライバシーポリシー / 運営者情報 / 特定商取引法に基づく表記 | すべて外部 LP の各アンカー (`#terms` / `#privacy` / `#operator-info` / `#tokushoho`)。`target="_blank"` |
| **ログイン後限定** | `isAuthenticated` のときのみ追加表示 | お知らせ / セキュリティ報告 | お知らせ = アプリ内 `/announcements` (next/link) / セキュリティ報告 = LP `#security` (`target="_blank"`) |

- **廃止 (ADR-0031)**: copyright (© 年 たすきば運営) / 最終更新日 / 「サービス情報」(`/settings/about` リンク)。
- **移設**: バージョン / 更新履歴 → ヘッダ右上 AccountMenu「バージョンアップ情報」(→ `/changelog`、ログイン後のみ。§20.2 参照)。
- `/settings/about` ページは移設により役割を失ったため削除済。
- 配色 / `mt-auto` 配置 / auto-hide 対象外などの実装詳細は [docs/design/UI_PATTERNS.md §38.7](../design/UI_PATTERNS.md) を参照。

---


## 個人メモ機能 (PR #70)

## 21. 個人メモ機能 (PR #70)

### 21.1 目的

各アカウントが業務上調査して得た知見・作業メモを保存する個人ストック。
既存の Knowledge はプロジェクト紐付け必須で共有前提だったため、
「**プロジェクトに紐付かない個人の知見置き場**」を新設する。

### 21.2 公開範囲 (visibility)

| 値 | 挙動 |
|---|---|
| `private` (既定) | 作成者のみ閲覧可。admin も含め他者は完全に閲覧不可 |
| `public` | 全メモ画面に公開、全ログインユーザが閲覧可 (編集/削除は作成者のみ) |

### 21.3 画面 (PR #71 で 2 画面構成に分離)

| 画面 | URL | 導線 | 表示内容 | 操作 |
|---|---|---|---|---|
| **メモ** (個人管理) | `/memos` | 右上ユーザ名プルダウン → メモ | 自分のメモのみ (private/public 両方) | 作成・編集・削除すべて可能 |
| **全メモ** (横断表示) | `/all-memos` | 上部ナビ (プロジェクト一覧と同列) | 全ユーザの public メモ (自分の公開メモを含む) | 行クリックで**参照専用**ダイアログ (read-only) |

- 両画面とも列幅はリサイズ可 (PR #68 機構、各画面で tableKey を分離)
- 全メモ画面で開く詳細ダイアログは `fieldset disabled` で全入力を読み取り専用化

### 21.3.1 一覧の列構成 (実装ミラー 2026-06-03)

他「○○一覧」と列構成・配置を統一。**リンク (url 型添付) と添付 (ファイル本体) を別列に分離**し、**公開範囲列は撤去**。

- **メモ (`/memos`)**: ☑(一括選択) / 件名 / 本文 / 作成日時 / 更新日時 / リンク / 添付 / 操作(削除)
- **全メモ (`/all-memos`)**: 件名 / 本文 / 作成者 / 作成日時 / 更新日時 / リンク / 添付 / 操作(削除・admin が他人 public のみ)

| 列 | 内容 | /memos | /all-memos |
|---|---|:--:|:--:|
| ☑ 一括選択 | エクスポート対象指定 + 公開範囲一括変更 | ○ | × |
| 件名 | メモタイトル (公開時必須) | ○ | ○ |
| 本文 | 本文の先頭抜粋 (ホバーで全文を `title` 属性表示) | ○ | ○ |
| 作成者 | 作成したユーザ名。/memos は自分のメモのみのため非表示 | × | ○ |
| 作成日時 | 作成日時 (`formatDateTimeSeconds` で秒まで) | ○ | ○ |
| 更新日時 | 更新日時 (秒まで)。作成と同値なら「—」 | ○ | ○ |
| リンク | 外部 URL 参照 (`storageProvider != 'supabase'`) を縦に列挙 (`LinksCell`) | ○ | ○ |
| 添付 | アップロードしたファイル本体 (`storageProvider == 'supabase'`) を chip 表示 (`AttachmentsCell`) | ○ | ○ |
| 操作 | 削除。/memos は本人、/all-memos は admin が他人 public のみ | ○ | △ (admin) |

> **★添付 (ファイル) 列★ (2026-06-03 更新)**: Memo も他資産と同様に `UPLOADABLE_ENTITY_TYPES` / `UPLOAD_ATTACHMENT_ENTITY_TYPES` に追加され、**ファイル本体のアップロードに対応** (旧「URL 添付のみ」制限を解除)。ファイルは「添付」列に chip 表示、外部 URL リンクは「リンク」列に表示される。ファイルアップロードは編集ダイアログ (`AttachmentList`) で行う (作成ダイアログは従来どおり URL リンクの staging のみ。他資産と同じ挙動)。書込認可は `authorizeMemoAttachment` (memo.userId === viewer 本人のみ)、ストレージ容量も他資産同様にテナントの使用量へ計上される。

- **更新者列は持たない**: メモは作成者本人のみ更新可能なため、更新者 = 作成者で冗長になる (他資産にある「更新者」列を意図的に省略)。
- **公開範囲は列から撤去**: /memos では一覧上部の一括変更ツールバー + 編集ダイアログで操作する。/all-memos は public のみ表示のため元から不要。
- **レイアウト順は他「○○一覧」と統一**: ① 件数 + エクスポート/インポート/メモ作成 (右上) → ② フィルター + 一括編集ツールバー → ③ 列幅リセット + テーブル。画面見出し (h2) は他一覧同様に持たない (2026-06-03 に撤去)。

### 21.4 操作

- **作成**: `/memos` 画面の右上「メモ作成」ボタン (ダイアログ)。全メモ画面には作成ボタンなし
- **編集/削除**: `/memos` 画面でのみ可能 (行クリック or 操作列ボタン)
- **URL リンク添付**: PR #64 の仕組みを拡張 (entityType='memo')。作成時に staging、編集時に即 CRUD。全メモ画面では閲覧のみ
- **ファイル添付 (2026-06-03 追加)**: memo も他資産同様にファイル本体アップロードに対応 (編集ダイアログ `AttachmentList`)。一覧では URL リンク = 「リンク」列、ファイル = 「添付」列に分離表示。書込は memo 本人のみ、ストレージ容量はテナント使用量へ計上

### 21.5 タグなし

業務知見として有用かは人間の目で判断するため、Knowledge のような tech/process/businessDomain タグは持たせない。
(将来 Knowledge 昇格機能で格上げ時に必要なら別 PR で追加)

### 21.5.1 提案エンジン候補化と embedding (2026-05-15 追加)

`visibility='public'` の Memo は他資産 (Knowledge / RiskIssue / Retrospective) と同様に **提案エンジンの候補** として扱われる。

- **embedding 生成**: `visibility='public'` で `title` / `content` のいずれかが変更されたタイミングで Voyage AI による embedding 生成 (`featureUnit='memo-embedding'`)。「自分のみ」(`visibility='private'`) は embedding 生成しない (= Voyage API 課金回避)
- **「自分のみ → 全メンバー」遷移時**: 初回 embedding 化される (= 公開化のタイミングで提案候補に乗る)
- **タグなし設計のため**、提案エンジンの 3 軸スコアのうち tagScore は常に 0。textScore + embeddingScore の 2 軸で実用ランキング
- **「なぜ?」説明文** (Pro プラン限定 / Phase 3) も Memo 対応 (candidateKind='memo')

詳細: [docs/specification/SUGGESTION_FEATURE.md §1.1](./SUGGESTION_FEATURE.md) / [docs/design/SUGGESTION_ENGINE.md](../design/SUGGESTION_ENGINE.md)

### 21.6 認可

- **閲覧**: 作成者 OR visibility='public' のユーザ。**admin 特権なし**
- **作成**: 認証済みユーザなら誰でも
- **編集**: 作成者のみ。**admin 特権なし**
- **削除**: 作成者のみ + **★2026-05-20 PR #416 追加★ admin による「他人 public メモ」削除のみ可能** (モデレーション特権)
  - 全メモ画面で admin が他人の public メモ行に「削除」ボタンを表示する
  - private メモは admin にも削除不可 (作成者本人のみ。情報漏洩防止)
  - 自分の memo は private/public いずれも本人がいつでも削除可
- 他人の private メモにアクセスしようとしても「存在しない」扱い (404) で情報漏洩を防止

### 21.7 API

| メソッド | パス | 認可 | 備考 |
|---|---|---|---|
| GET | /api/memos?scope=mine | 認証済みユーザ | 既定。自分のメモのみ |
| GET | /api/memos?scope=public | 認証済みユーザ | 全ユーザの public メモ (PR #71) |
| POST | /api/memos | 認証済みユーザ | |
| GET | /api/memos/:id | 閲覧権限あるユーザのみ (本人 or public) | |
| PATCH | /api/memos/:id | 作成者のみ | |
| DELETE | /api/memos/:id | 作成者 OR admin (他人 public のみ) | PR #416 で admin モデレーション削除を追加。admin でも他人 private は不可 |

### 21.8 添付 (PR #64 連携 / 2026-06-03 ファイル本体対応)

- `entityType='memo'` を ATTACHMENT_ENTITY_TYPES に追加
- memo は project スコープ外のため、attachment の認可は project membership ではなく memo.userId + visibility で判定
- `authorizeMemoAttachment(memoId, viewerUserId, mode)` を新設 (read/write で権限が異なる。write = memo.userId === viewer 本人のみ)
- batch 取得 API は閲覧可能 memo の ID にフィルタしてから attachments を返す (URL 漏洩防止)
- **URL リンク添付** (PR #64): 作成時 staging / 編集時 即 CRUD
- **ファイル本体アップロード (2026-06-03)**: memo を `UPLOAD_ATTACHMENT_ENTITY_TYPES` (validator) と `UPLOADABLE_ENTITY_TYPES` (UI) に追加し、他資産同様に Pre-signed URL アップロードに対応 (旧「URL 添付のみ」制限を解除)。`storageProvider='supabase'` で保存され、一覧「添付」列に表示。ストレージ使用量・Beginner 100MB 無料枠判定も他資産と共通

---


## 日付項目の入力効率化 (PR #71 追加)

## 22. 日付項目の入力効率化 (PR #71 追加)

### 22.1 変更内容

全ての日付入力項目 (予定開始日 / 終了日・実施日・期限 等) の**右隣**に「**今日**」「**削除**」ボタンを常時表示。

| ボタン | 動作 |
|---|---|
| 今日 | 1 クリックで今日日付 (ローカルタイム YYYY-MM-DD) をセット |
| 削除 | 1 クリックで値をクリア (空欄化)。未入力時は disabled |

必須項目 (required) では「削除」ボタンを非表示にする (空にできない項目で混乱を招かないため)。

### 22.2 導入画面

- プロジェクト新規作成 / 編集 (開始・終了予定日)
- WBS 新規作成 / 個別編集 / bulk edit / bulk actual (予定・実績の各日付)
- 振り返り作成 / 編集 (実施日)
- リスク/課題 編集 (期限)

### 22.3 背景

ブラウザ標準 `<input type="date">` では「今日」「削除」相当操作がカレンダーポップオーバー内部にしかなく、2 ステップ操作 (開く → 探す) が必要だった。ボタン常時露出により 1 ステップ化。

### 22.4 自作カレンダーへの置換 (PR #72)

右側に「今日」「削除」ボタンを外出ししたことで、カレンダーポップアップ内部のベンダー製
「今日」「クリア」ボタンは役割が重複し、UI の一貫性を損ねていた。しかし native `<input type="date">`
ではそれらを CSS / JS で非表示にできない。

よって PR #72 で native input を廃止し、自作の Popover + 日付グリッドに置換した:

- **カレンダー内には月ナビと日付グリッドのみ** (今日 / クリアは出現しない)
- 外側の「今日」「削除」ボタンは PR #71 と同じ仕様で維持
- value / onChange は引き続き `YYYY-MM-DD` 文字列 (呼び出し側 11 箇所に変更なし)

---


## アカウント情報 (feat/settings-tenant-identity 2026-05-21 追加)

### 22.A 表示する項目

ユーザ個別設定 `/settings` の最上部に「アカウント情報」セクションを配置。一般ユーザでも、次回ログイン時に必要な組織 ID や自身の登録情報を自己解決で確認できる。

| 表示項目 | バックエンド | 編集 | 用途 |
|---|---|---|---|
| 組織 ID | `Tenant.slug` | × | ログイン画面の「組織 ID」欄に入力する値 (ADR-0016) |
| テナント表示名 | `Tenant.name` | × | 自分の所属組織の認識 |
| メールアドレス | `User.email` | × | ログイン入力 + パスワードリセット受信先 |
| 氏名 | `User.name` | × | 本人確認 |
| ロール | `User.systemRole` | × | super_admin / admin / general を業務文言バッジで表示 |
| 最終ログイン | `User.lastLoginAt` | × | セキュリティ確認 (見覚えのないログインを発見できる)。`formatDateTimeFull` (分まで) |
| MFA 有効化日時 | `User.mfaEnabledAt` | × | 多要素認証を有効化した日時 (null=未設定→「未設定」表示)。`formatDateTimeFull` |
| アカウント作成日時 | `User.createdAt` | × | アカウント発行日時。`formatDateTimeSeconds` (秒まで) |

### 22.A.1 設計判断

- テナント識別子は **`slug` + `name` のみ** を載せる。`tenantSeq` / UUID は管理者向けの `/settings/tenant` に留め、一般ユーザの認知負荷を上げない (ADR-0018)。
- 解約済テナント (`Tenant.deletedAt` セット) のユーザでは本セクションを描画しない (`getUserSelfAccountInfo()` が null 返却)。
- PR #420 で追加された localStorage 履歴 (LRU 5 件 / 90 日 expire) と相補的に機能: 履歴がない・履歴クリア後・別ブラウザでも組織 ID を確認できる。

### 22.A.2 関連

- ADR: [ADR-0018](../adr/0018-tenant-identifier-user-visibility.md)
- サービス: `src/services/user-self.service.ts` (`getUserSelfAccountInfo()`)
- 画面: `src/app/(dashboard)/settings/settings-client.tsx` (`AccountInfoProp` セクション)

---

## 画面テーマ設定 (PR #72 追加)

## 23. 画面テーマ設定 (PR #72 追加)

### 23.1 設定画面での選択肢

**設定 → 画面テーマ**に 10 種のラジオ選択肢を配置。

| 大分類 | バリアント |
|---|---|
| ライト / ダーク | ライト (既定) / ダーク |
| パステル (低彩度) | 青 / 緑 / 黄 / 赤 |
| ポップ (高彩度) | 青 / 緑 / 黄 / 赤 |

### 23.2 既定値と永続化

- 既定は **ライトテーマ** (既存挙動と同じ見た目)
- ユーザが変更した値は `users.theme_preference` カラムに保存 → ログイン / ログアウト / ブラウザ再起動を跨いでも維持される
- 未ログイン画面 (ログイン・パスワード再設定等) は常にライトテーマ

### 23.3 反映タイミング

テーマ変更は即時反映 (ページリロード不要):

1. 設定画面で選択
2. `PATCH /api/settings/theme` で DB 更新
3. セッション JWT 更新 + `router.refresh()` でルートレイアウトを再レンダリング
4. `<html data-theme=...>` が書き換わり全画面の配色が切り替わる

### 23.4 セキュリティ

- API サーバは `THEMES` 定数に含まれるキー以外を拒否 (400 VALIDATION_ERROR)
- クライアントからの任意文字列による CSS 注入・データ汚染を防ぐ
- セッション JWT 上のテーマ値を layout 表示前に `toSafeThemeId()` で丸める (二重防御)


## 言語・タイムゾーン設定 (PR #118/#119/#120 追加)

## 24. 言語・タイムゾーン設定 (PR #118/#119/#120 → PR-1 #327 でテナント単位へ移管)

> **★2026 実装ミラー★ 配置場所の移管 (PR-1 #327)**: 言語・タイムゾーンは当初 **個人設定 `/settings`** にユーザ単位 (`users.timezone`/`users.locale`) で配置していたが、**テナント設定 `/settings/tenant` (概要タブ、admin のみ)** に移管され **テナント単位 (`tenants.timezone`/`tenants.locale`) で運用**する方針に変更 (同一テナント内のユーザ間で日付計算がズレないようにするため)。
> 現在、**個人設定画面 (`/settings`) には言語・タイムゾーンの UI は無い** (= アカウント情報 / 画面テーマ / パスワード変更 / MFA の 4 セクションのみ)。以下 24.1〜24.4 の挙動 (選択肢・既定値・反映・UTC 格納) は**テナント設定での設定**に読み替えること。

### 24.1 設定画面での選択肢 (現: テナント設定 `/settings/tenant` 概要タブ・admin のみ)

2 つのセレクトボックスを配置:

- **言語**: `SUPPORTED_LOCALES` (`src/config/i18n.ts`) のキー + 「システム既定を使用」
  - 現在 **選択可能** (`SELECTABLE_LOCALES=true`): `ja-JP` のみ
  - 表示するが **選択不可** (`SELECTABLE_LOCALES=false`): `en-US` (※準備中、PR #120 で制御)
    - UI: `<option disabled>` + 「※準備中」表記
    - API: `isSelectableLocale` で 400 拒否 (UI 迂回 / curl 直叩き防止)
  - 後続 PR で `src/i18n/messages/en-US.json` + UI 文字列抽出 + 英訳が完了次第、`SELECTABLE_LOCALES['en-US']` を true に切り替えて選択可にする
- **タイムゾーン**: `Intl.supportedValuesOf('timeZone')` で動的生成した IANA 名一覧 + 「システム既定を使用」
  - ブラウザ非対応時は代表値 (UTC, Asia/Tokyo, America/New_York 等) の fallback

### 24.2 既定値と永続化

- 既定は両方とも「システム既定を使用」(= null 保存) → `src/config/i18n.ts` の `DEFAULT_TIMEZONE` / `DEFAULT_LOCALE` (env で上書き可) が適用
- ユーザが変更した値は `users.timezone` / `users.locale` に保存 (PR #118 マイグレーション済)
- null へ戻すことも可能 (UI 上「システム既定を使用」を選択)

### 24.3 反映タイミング

1. 設定画面で選択 → 「保存」押下
2. `PATCH /api/settings/i18n` で DB 更新
3. `useSession().update({ timezone, locale })` で JWT 再発行
4. `router.refresh()` でサーバコンポーネント再描画 + `useFormatters()` が新値を反映
5. 全画面の日時表記が即時切り替わる (`2026/04/24 09:00` → `04/24/2026 00:00` 等)

### 24.4 データ格納方針

- **DB**: 常に UTC (`timestamptz`) で格納 / API 入出力は ISO 8601 UTC (`...Z`)
- **描画**: `formatDate` / `formatDateTime` / `formatDateTimeFull` ヘルパ経由で描画時に解決
- **入力**: 現時点はユーザ TZ で解釈する実装 (date-picker 入力時) は **未整備** (PR #121 予定)

### 24.5 セキュリティ

- API サーバは以下のみ受理 (400 VALIDATION_ERROR で拒否):
  - `timezone`: `Intl.DateTimeFormat` が受理する IANA 名、または `null`
  - `locale`: **`SELECTABLE_LOCALES=true` のキー** (PR #120 で厳格化)、または `null`
    - `SUPPORTED_LOCALES` に含まれていても `SELECTABLE_LOCALES=false` (例 `en-US`) は拒否
    - 既に DB に `en-US` が入っている可能性は無いが、将来的にロケールを削除する場合も
      `isSupportedLocale` はフォールバックが effect せず `resolveLocale` は FALLBACK ロケールに戻る設計
- 任意文字列による DB 汚染 / 未整備ロケールへの誘導を防ぐ (UI disabled との多層防御)
- 本人のみ更新可 (`/api/settings/i18n` は `getAuthenticatedUser` のみ要件、他ユーザ設定は触れない)

## 25. 他の端末からログアウト (feat/logout-other-devices 2026-06-03)

### 25.1 目的

個人設定 `/settings` の最下部 (MFA セクションの下) に「他の端末からログアウト」セクションを配置。端末紛失・共用 PC への置き忘れ等に備え、**ユーザ本人が、現在の端末以外のすべてのログインセッションを即時無効化**できる自己防衛機能。

### 25.2 挙動 (`POST /api/auth/logout-other-devices`)

1. `user.tokenVersion` を increment → **全端末** (現在端末を含む) の既存 JWT が失効 (api-helpers / page-auth の tokenVersion 照合で 401)。
2. **呼出端末のみ**、新 tokenVersion で JWT を再署名 (`reissueAuthJwtOnResponse({ tokenVersion })`) して Set-Cookie → 現在端末はログイン維持。
3. 他端末は旧 tokenVersion のまま → 次回リクエストで自動ログアウト。
4. `authEventLog` に `eventType='logout_other_devices'` を記録。

- **認可**: 認証済みユーザ本人のみ。自分の `tokenVersion` のみ操作し、他人のセッションには影響しない。
- **middleware 除外**: `api/auth/logout-other-devices` を matcher 除外に追加 (auth() auto-refresh による再署名 cookie 上書きを回避。`explicit-signout` / `mfa/verify` / `i18n` と同じ理由)。
- **フェイルセーフ**: 再署名に失敗した場合、現在端末も旧 tokenVersion のままとなり次回リクエストでログアウトされる (= 最悪でも全端末ログアウト)。この場合 500 を返し「再ログインが必要」と通知。
- **通常ログアウトとの違い**: 通常ログアウト (`explicit-signout`) も tokenVersion increment で全端末を失効させるが、現在端末の cookie も削除してログイン画面へ遷移する。本機能は**現在端末のログインを維持**したまま他端末のみ締め出す点が異なる。

### 25.3 関連
- 実装: `src/app/api/auth/logout-other-devices/route.ts` / `src/app/(dashboard)/settings/settings-client.tsx` (`handleLogoutOtherDevices`)
- JWT 再署名: `src/lib/auth-jwt-helper.ts` (`tokenVersion` patch)
- セッション失効設計: [SECURITY.md §9.17 / JWT 失効ガード](../design/SECURITY.md)

